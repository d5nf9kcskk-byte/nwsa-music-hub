# UI Redesign Study — Spotify + Six SaaS References

**Status:** study only — no application code changed.
**Branch:** `claude/ui-redesign-spotify-study-3buf5v`
**Date:** July 2026

The owner asked what it would take to redesign NWSA Music Hub's UI and layout
using Spotify as the primary layout/flow/wireframe reference, with Basecamp,
Airbnb, Asana, Slack, HubSpot, and QuickBooks Online as additional examples —
and to surface any significant usability roadblocks *before* implementing
anything.

## 1. Method

A 25-agent study, run in five phases:

1. **Audit** — four parallel readers mapped the public surface, director
   surface, design system/CSS, and data model of the actual codebase.
2. **Reference teardowns** — eight researchers produced wireframe-grade
   anatomies of Spotify (desktop + mobile, including its documented usability
   criticisms), Basecamp, Airbnb, Asana, Slack, HubSpot, and QuickBooks Online.
3. **Concepts** — four independent designers produced competing redesigns:
   full Spotify adoption ("Encore"), Spotify-structure/NWSA-skin hybrid
   ("Front of House / Backstage"), a utility-SaaS shell ("Desk & Door"), and a
   conservative refresh ("Same Stage, New Lighting").
4. **Judging** — three judges scored all concepts on family usability,
   director efficiency, value-per-effort, accessibility, and brand fit, each
   weighting a different priority (families / director / engineering realism).
5. **Adversarial critique** — six critics attacked the winning concept from
   the lenses: non-technical parent via QR code, hurried student, director
   mid-rehearsal, accessibility + EN/ES, content/artwork reality, and
   engineering migration risk. Their findings are the roadblock register in §7.

## 2. The app today (audit digest)

Both surfaces are **phone-first hamburger apps**; there is no desktop layout
anywhere in ~5,900 lines of plain CSS (only a 640px overlay tweak). That is
simultaneously the largest gap versus every reference product and the largest
opportunity: desktop is a blank canvas, so a sidebar shell can ship without
disturbing the phone experience families already know.

- **Public ("the Door")** — 16 screens, no login. Sticky teal header
  (EN/ES toggle, "Aa" text-size control, search, hamburger), slide-in menu,
  4-tab bottom bar (Home / Calendar / My Schedule / Concerts), full-screen
  fuzzy search. Identity is device-local localStorage (saved student, parent
  mode with multiple kids) — no accounts. Exactly one public Firestore write:
  planned-absence reports.
- **Director ("the Desk")** — 12 views behind auth, tab-in-URL
  (`/director/<tab>?ensemble=…`). Three views (Who's Out, Subs & Pull-outs,
  Ensemble hub) are unreachable from the menu today — real nav debt.
- **Load-bearing constraints** a redesign must not break:
  four **printed** surfaces (concert program, season "fridge copy", start
  guide, QR kit); QR/vanity short links on paper posters (`/so /we /wind /jazz
  /cam /choir /opera /cco`); static ICS feed URLs; EN/ES i18n; the "Aa"
  text-size control (implemented as CSS `zoom` on `.pub-app`); offline PWA
  service worker (`nwsa-hub-v1`); the code-split boundary (public users never
  download director code); deep links (`?ensemble=`, `?focus=`, `#anchors`).
- **Latent bugs the audit surfaced en passant:** the top ~727 lines of
  `src/index.css` are a dead dark-purple theme from an earlier app iteration
  (`.app`, `.bottom-nav`, `.exercise-*`); print styles force white backgrounds
  but not text colors (a dark-mode-print "invisible ink" risk); the z-index
  ladder is ad hoc (30–400).

## 3. What each reference contributes

| Reference | Contribution to the design |
|---|---|
| **Basecamp** | The one-page **ensemble hub** (tool tiles: announcements, schedule, repertoire, seating, roster) and the anti-sprawl rule: public nav is five destinations and never grows. Calm-notification philosophy. |
| **Airbnb** | The event page as a listing: sectioned content + a **sticky day-sheet action card** (call time · dress · venue · Add to calendar) that never scrolls away; mobile bottom action bar; "Show all N" progressive disclosure; click-to-activate maps with exact pins. |
| **Asana** | **Slide-in record panes** (open a student without losing roster scroll; Esc closes; URL deep-links) and fixed multi-view tabs over one dataset for the director ensemble page. Fixed home widget grid. |
| **Slack** | Ensembles-as-channels sidebar rows with color dots and quiet "changed since you last looked" indicators; Cmd+K quick switcher; the isomorphism rule (sidebar and tab bar hold the same items). |
| **HubSpot** | **Saved-view index tables** for the roster (All / per-ensemble / Seniors / Missing info), bulk-action bar, and record anatomy (identity card + timeline + associations). Educational empty states. |
| **QuickBooks Online** | The director shell: grouped collapsible left rail (≤6 groups, click-not-hover), the universal **"+ New"** create menu with return-to-context, the money-bar reborn as the Take-Roll status strip, review-queue framing for follow-ups, settings split behind a gear. |
| **Spotify** | The piece/album page (movements = tracklist, program notes = liner notes), the deterministic gradient "cover art" system seeded from `ensembleColor`, the happening-now mini-bar in the mini-player slot — plus its documented failures used as an **anti-pattern checklist** (dark low-contrast chrome, hover-only affordances, carousels that hide data, unstable layouts, weak information scent for goal-directed lookup). |

## 4. Four concepts, three judges

| Concept | Angle | Family judge | Director judge | Engineering judge |
|---|---|---|---|---|
| Encore | Full Spotify shell, dark theme | 21 | 24 | 31 |
| Front of House / Backstage | Spotify structure, NWSA skin | 2nd | 2nd | 41 |
| **Desk & Door** ★ | Utility-SaaS shells + Spotify moments | **win** | **win** | 40 |
| Same Stage, New Lighting | Conservative refresh | 3rd | 3rd | **42 (win)** |

The family-usability and director-efficiency judges both picked **Desk & Door**.
The engineering judge narrowly preferred the conservative refresh on blast-radius
grounds; its de-risking release plan was grafted into Desk & Door as Phase 0,
along with ~30 other graft ideas voted in from the losing concepts (deterministic
cover-art component, director session bar, mounting all new fixed chrome outside
the text-zoom subtree, luminance-aware text color over ensemble gradients, etc.).

Why the losers lost, in one line each: **Encore** put times and rooms into
exactly the low-contrast dark register Spotify is criticized for, for the users
least able to tolerate it; **Front of House** was strong but second everywhere —
a structure transplant that still inherits browsing-first assumptions;
**Same Stage** is the best value-per-effort but leaves the director console's
nav debt and desktop absence unaddressed.

## 5. Recommended design: "Desk & Door"

### Thesis

NWSA Music Hub's two real jobs — a parent answering "where is my kid supposed to be, and did anything change?" and a director marking roll for 80 students one-handed mid-rehearsal — are utility jobs, not browsing jobs. So the shells come from the six SaaS references: the public surface (the "Door") becomes a Basecamp-calm, document-first site with a fixed five-destination nav and Airbnb-style detail pages whose one next action (Add to calendar / Get directions) is always pinned on screen; the director console (the "Desk") becomes a QuickBooks-Online-style working shell — grouped left rail, universal "+ New", dense HubSpot saved-view tables, Asana multi-view ensemble pages and slide-in record panes. Spotify is applied only where the content is literally music and its documented failures can't hurt: the piece page becomes a genuine track/album page, ensemble brand colors become Spotify-style gradient "cover art" heroes (the data model has zero images), and NowNext consolidates into a persistent "happening now" mini-bar in the now-playing slot. Spotify's audited anti-patterns — horizontal carousels that hide data, hover-only affordances, artwork-first grids without artwork, algorithmically unstable layouts, dark-only low-contrast chrome — are explicitly designed out, because every one of them attacks exactly what a schedule app must guarantee: completeness, predictability, and legibility (including in print).

### Information architecture

== GOVERNING RULES ==
(1) Basecamp anti-sprawl rule: the public nav is five destinations and never grows; new capability lands as a tile inside an ensemble hub, never as a new nav item. (2) Slack isomorphism rule: desktop sidebar items and mobile bottom tabs are the SAME items — nothing relocates conceptually between devices, only spatially. (3) Zero URL churn: router basename /nwsa-music-hub, all routes, vanity slugs (/so /we /wind /jazz /cam /choir /opera /cco), hash anchors (/ensemble/:id#repertoire, /map#anchor), query deep links (?focus=, ?ensemble=, ?staff=1), and ICS feed URLs ({origin}/nwsa-music-hub/feeds/*.ics) are contract-frozen. (4) One sticky element per screen edge per breakpoint (Airbnb discipline), with a formalized z-index scale replacing today's ad hoc 30–400 ladder.

== PUBLIC SURFACE ("the Door" — no login, mobile-first) ==
TOP-LEVEL DESTINATIONS (fixed): 1. Today (/, the bounded home feed) · 2. Calendar (/calendar) · 3. My Schedule (/student/:id via /lookup) · 4. Concerts (/concerts season page) · 5. Ensembles (/ensembles index → /ensemble/:id hubs).
SECONDARY "RESOURCES" SET (menu/sidebar section, not tabs): Announcements, Repertoire, Assignments, Start Here, Campus Map.
HIERARCHY: Today → (alerts, today's events, coming up, ensembles grid, all-caught-up end marker) → Ensemble hub (Basecamp one-page tool-tile hub: Announcements, Schedule, Repertoire, Seating, Roster, Subscribe) → detail pages: Event (Airbnb detail skeleton + sticky day-sheet action panel), Piece (Spotify track page), Program (print-sacred document — untouched layout, masthead stays exactly "New World School of the Arts").
CROSS-CUTTING LAYERS: Identity (localStorage remember-me + parent mode) surfaces as a saved-student switcher pinned at the sidebar bottom / in the mobile menu — Basecamp "My Stuff": My Schedule aggregates rehearsals, practice card, parts, planned-absence write. GlobalAlerts (urgent/cancellation/all-clear strips) stays site-wide at top of content on every page — never demoted. NEW: an optional "Updates" view (announcements + schedule changes interleaved, each row leading with ensemble name + change type, "since your last visit" divider computed from a localStorage last-seen timestamp — Slack Activity, corrected per its criticism). NEW: "Happening now" mini-bar (NowNext consolidation) docked in the Spotify now-playing slot, rendered only when a saved student has a NOW/NEXT event today.
WHAT MOVES WHERE: the hamburger menu's route list → desktop pinned sidebar; on mobile the hamburger survives but slims to Resources + ensembles accordion + identity + director login. SearchOverlay is unchanged on mobile; on desktop it gains a persistent header search field (Slack top bar) opening the same overlay as an anchored palette. Nothing else moves; the four bottom tabs are frozen (Spotify tab-churn lesson).

== DIRECTOR CONSOLE ("the Desk" — auth-gated) ==
NAV GROUPS (QBO grouped rail, ≤6 groups, click-to-expand accordion — never hover): 
1. Today (dashboard) · 2. Attendance → Take Roll, Who's Out, Tracker · 3. Schedule → Calendar, Schedule Change (swap), Subs & Pull-outs · 4. People → Roster, Progress Notes · 5. Library → Repertoire, Assignments, Announcements · 6. ENSEMBLES section: one color-dot row per ensemble (Slack sidebar), each opening the Ensemble page.
This surfaces the three currently menu-orphaned tabs (whosOut, scheduleChanges, ensembleHub) as first-class destinations while preserving the DirNavigate go()/intent-param cross-nav contract.
ENSEMBLE PAGE: Asana multi-view tabs over one data store — Overview / Roster / Attendance / Schedule / Repertoire / Seating / Program. Fixed tab set, no user-created views (Asana's clutter lesson). SeatingManager lives here.
"+ NEW" (QBO flagship): pinned pill above the rail, opening a two-column grouped create menu — SCHEDULE: Rehearsal, Concert, Event, Generate rehearsals… · PEOPLE: Student, Sub/Pull-out · COMMS: Announcement, Progress note · LIBRARY: Piece, Assignment. Selecting opens the existing form as a right slide-in (desktop) / bottom sheet (mobile); closing returns to prior context.
GEAR (QBO settings split — setup out of daily nav): Locations, ICS import, seeders, QR Kit, Season Checklist + graduate-seniors, sign-out. 
RECORDS: Student and Event get HubSpot record anatomy delivered via an Asana slide-in side pane on desktop (list stays visible; Esc closes; URL deep-links) and a full-screen sheet on mobile.
GLOBAL: DirectorSearch = Cmd+K quick switcher (already built; gains shortcut + persistent field). WriteTray (undo/retry) and the header saving/saved cue keep permanently reserved slots in the new chrome. QR kit, print outputs, notifyQueue relay, documented-reason invariant: unchanged.

### Desktop layout

== PUBLIC DESKTOP (breakpoint ≥1024px; today no desktop layout exists — this is all new) ==
FRAME: CSS grid, rows [header 56px | content 1fr | happening-now bar 48px when live], columns [sidebar 264px | main 1fr]. Header and sidebar live OUTSIDE the .pub-app text-zoom subtree (or ship after the px→rem pass) so the "Aa" control scales content only. Light theme, warm neutral background, white cards (Basecamp calm — NOT Spotify dark; print + parent legibility win).
HEADER (full-width, teal-gradient brand bar retained): left — "NWSA Music Hub" wordmark linking home; center-left — persistent search input, 320–480px, placeholder "Search events, ensembles, music…", opens the existing SearchOverlay as an anchored command palette (Slack top bar); right — EN|ES pill, "Aa" text-size menu, quiet "Director" login link.
SIDEBAR (light panel, own scroll): Section A — five primary rows (icon + label, 44px, active = filled highlight + 3px left accent, QBO active-state grammar): Today, Calendar, My Schedule, Concerts, Ensembles. Section B — "ENSEMBLES" header + one row per ensemble: 10px color dot + name; row renders bold with a small dot when that ensemble has announcements/schedule changes newer than the device's last-seen timestamp (Slack unread semantics, localStorage-driven). Section C — "RESOURCES": Announcements, Repertoire, Assignments, Start Here, Campus Map. Pinned bottom: saved-student chips (parent-mode quick switch, "Not you?" link) + "Subscribe to calendar" shortcut. Sidebar hidden under @media print.
MAIN: content column max 860px centered for list pages, 720px for reading pages (Basecamp document measure). GlobalAlerts strips render at the top of main on every route. HOME: vertical bounded sections in fixed order (date hero → schedule-change banner → announcements → today's events → find-my-schedule CTA → Coming up → ensemble tile grid) ending in an explicit "You're all caught up" row; no horizontal carousels anywhere (NN/g + the repo's own reverted swipe-strip finding).
EVENT DETAIL (Airbnb listing skeleton): H1 title row with right-aligned "Share / Save" actions → cancellation/change banner slot → two-column body: LEFT ~58% stacked sections with 1px dividers (What & who, Linked pieces with "Show all N", Notes, Where — embedded campus map with click-to-activate guard + exact pin + "Open in Google Maps"); RIGHT ~360px STICKY DAY-SHEET CARD (top: 72px; bounded stickiness ending before the map section): date/time headline, Call time / Downbeat / Dress / Pickup rows, venue + address, primary CTA "Add to calendar" (.ics), secondary "Get directions", tertiary "View program". Bottom: "Things to know" 3-column grid (Arrive by / What to wear / Pickup).
ENSEMBLE HUB: full-width hero band ~220px — gradient generated from ensembleColor (Spotify detail-page hero; the artwork substitute) containing ensemble name (large), next-event line, member count, and a "Subscribe" pill (= Follow → ICS wizard). Below: Basecamp tool-tile grid, 2-up, each tile = heading + live preview + "Show all N": Announcements, Upcoming rehearsals, Concerts, Repertoire (top 5 pieces), Seating charts, Roster.
PIECE PAGE (the Spotify moment): gradient hero from owning ensemble's color — type label "REPERTOIRE", full title 32–48px, composer + dates, duration; action row: "My part" (starred per saved identity), Listen / Watch / IMSLP buttons; body: movements as a numbered tracklist table (# / title / duration), instrumentation, program notes as an "About" reading block, "Programmed for" concert links.
HAPPENING-NOW BAR (only when live): slim full-width strip docked at viewport bottom: ensemble color dot, "NOW · Symphony Orchestra rehearsal · Room 210 · until 4:30" (or NEXT + countdown), whole bar links to the event; dismissible; hidden in print.

== DIRECTOR DESKTOP (breakpoint ≥1024px) ==
FRAME: QBO three-zone shell — rows [top bar 56px | content 1fr], columns [rail 240px, collapsible to 64px icon rail with click-opened flyouts | content well 1fr, pale-gray background, white cards].
TOP BAR: left — current page title + breadcrumb (Ensembles ▸ Jazz Band ▸ Roster); center-right — saving/saved cue (useWriteBusy), search field (Cmd+K → DirectorSearch); right — gear (setup menu), public-site link, avatar/sign-out.
RAIL: "+ New" pill (brand teal, full rail width) at very top → two-column grouped create menu overlaying content; then the six nav groups as accordion rows (one group open at a time); then ENSEMBLES color-dot list; bottom: collapse toggle (state remembered).
TODAY: Asana Home — date + greeting band; FIXED two-column widget grid (no user customization): [Today's schedule with roll receipts + expected counts | "Roll not taken" alert card] / [Unexcused follow-up queue | Announcements] / [Coming up | Quick actions + Season checklist card]. Every row deep-links via existing DirNavigate.
ROSTER (HubSpot index page): header row — "Students · 94" + Import + primary "+ Add student"; saved-view tabs (All, one per ensemble, Seniors, Missing info); toolbar — filter dropdowns (ensemble, instrument, grade, status), search-within, column gear (show/hide, compact density — Slack compact mode); TABLE: 44px rows, frozen checkbox + name columns, sortable headers, row hover reveals "Preview" (opens side pane) and per-row primary verb + caret (QBO action column: "Open · ▾ Log absence / Add note / Schedule change"); checked rows summon a bulk bar (assign ensemble, email parents, graduate).
STUDENT/EVENT RECORD (Asana side pane, HubSpot content): slide-in from right, ~480px, resizable, list remains interactive; arrow keys move selection beneath and the pane follows; Esc closes; URL updates. Pane: identity card (name, preferred name/pronunciation, instrument/section, grade, ensemble chips) + quick-action row (Log absence, Add note, Call, Email — auth-only contacts data); tabs: Timeline (attendance history + notes + planned absences, chronological, filter chips by type, "Upcoming" pinned) | Ensembles & parts | Assignments.
TAKE ROLL: full-width dense list (no cards padding-out 80 rows); sticky STATUS STRIP at top of content (QBO money bar): segmented Present / Absent / Late / Excused / Lesson / Unmarked counts, each segment tap-filters the list; keyboard: ↑/↓ row navigation + single keys A/L/E/S(lesson)/Space-present + Enter advance (Asana chord ethos, cheat-sheet overlay on ?); planned-absence hints, history dots, section-gap warnings, seating tap-roll, sticky bottom "Finish roll" bar all retained; absentee summary keeps tel:/mailto:/copy-for-Teams.
SCHEDULE: two-pane — month grid (existing, gains room) left ~60%, sticky selected-day panel right ~380px listing that day's event cards with Take Roll / Who's out / Program buttons; EventForm and all managers open as right slide-ins on desktop (bottom sheets remain on mobile); concurrent-edit guard, conflict radar, PiecePicker unchanged.
WHO'S OUT / TRACKER: read-only dense tables in the content well with date/ensemble controls in a toolbar row.
RESERVED SLOTS: WriteTray toasts bottom-center of the content well; nothing else may dock there. QR Kit and all print outputs render chrome-free.

### Mobile layout

== PUBLIC MOBILE (<744px — deliberately conservative; this surface already works and is majority-phone) ==
TOP: sticky teal header (brand link, EN|ES pill, "Aa" menu, search icon → full-screen SearchOverlay, hamburger). GlobalAlerts strips directly beneath on every page (urgent / cancellation / all-clear — unchanged semantics).
CONTENT: single column, max 720px; bounded vertical sections; no horizontal shelves. Home order identical to desktop (Slack isomorphism), ending in "all caught up".
BOTTOM: 4-tab bar frozen as-is — Home / Calendar / My Schedule (→ /student/:id or /lookup) / Concerts — 52px + safe-area. HAPPENING-NOW MINI-BAR: slim strip docked immediately above the tab bar (Spotify mini-player slot), rendered only when NowNext has content for the saved student: color dot + "NOW · Rehearsal · Room 210" + chevron; the SW update toast shares this anchor (bottom: 76px + safe-area) and wins when both would show.
DETAIL PAGES (Airbnb mobile rule — contextual action bar replaces global nav): on /event/:id the tab bar is swapped for a sticky bottom action bar: LEFT stack "Fri Dec 12 · 7:00 PM" + venue name (tap scrolls to day sheet), RIGHT primary button "Add to calendar"; back chevron top-left (history-aware navigate(-1) preserved). Ensemble hub: gradient hero (compact, ~140px) then tool tiles stacked single-column in desktop order, each with "Show all N". Piece page stacks hero → My part/Listen actions → movements tracklist → instrumentation → program notes. Program page: untouched print-first document.
MENU (hamburger, right slide-in, retained): Resources links, ensembles accordion with color dots, saved-student switcher + parent mode, Start Here, Director login. Lookup flow (A–Z rail, "Is this you?", parent mode) unchanged. PlannedAbsence modal, subscribe bottom-sheet wizard, month-swipe calendar: all unchanged.

== DIRECTOR MOBILE (phone, one-hand rehearsal use is the primary ergonomic target — do not degrade) ==
FRAME: current 100dvh flex column kept — sticky header (page title + NWSA mark, saving/saved cue, search icon, hamburger), scrolling content, safe-area insets.
NAV: hamburger menu reorganized to mirror the desktop rail groups exactly: Today · Attendance (Take Roll, Who's Out, Tracker) · Schedule (Calendar, Schedule Change, Subs & Pull-outs) · People (Roster, Progress Notes) · Library (Repertoire, Assignments, Announcements) · Ensembles color-dot list · gear items (Locations, QR Kit, Season Checklist, ICS import) · public-site link · sign-out. The three orphan tabs become reachable on phone for the first time.
CREATE: the purple FAB becomes the "+ New" surface (QBO mobile pattern): tap opens a grouped bottom sheet with the same create targets as desktop; forms remain the proven dir-drawer bottom sheets (90dvh, handle, footer Save/Cancel, focus trap).
TAKE ROLL: unchanged large-tap-target list, sticky top summary, sticky bottom "Finish roll" bar; ADDS the status-strip chips (Present/Absent/Late/Excused/Lesson/Unmarked) as a wrapping tap-to-filter row above the list — no keyboard dependency on phone.
RECORDS: what is a side pane on desktop is a full-screen pushed sheet with a back affordance here (Asana mobile rule); student sheet = identity card + quick actions + timeline tabs.
TABLES → CARDS: HubSpot/QBO tables linearize per the explicit column-priority order set by the desktop column gear (QBO lesson: columns→cards via priority, no silent column loss).
RESERVED SLOTS: WriteTray above bottom chrome; FAB bottom-right; only one of {FAB, finish-roll bar} per screen. All print surfaces (QR posters, folder slips, program) unchanged and chrome-free under @media print.

### Pattern mapping (app concept ← reference pattern)

- **Public ensemble page (/ensemble/:id)** ← Basecamp one-page project hub with live-preview tool tiles: It answers a parent's only structural question — 'where does everything for Jazz Band live?' — on one screen; tiles (Announcements, Schedule, Repertoire, Seating, Roster, Subscribe) map 1:1 to sections the page already has, and directors can conceptually toggle tiles per ensemble instead of requesting new nav items.
- **Public global navigation governance (5 fixed destinations + Resources)** ← Basecamp fixed seven-item top nav that never grows: Parents are untrained occasional users; a nav that never changes builds the muscle memory Spotify's constantly rearranged Home destroys. New features become hub tiles, not nav entries.
- **Announcement reading pages and the printed concert program** ← Basecamp Message Board document typography (~700px measure, large humanist body type, byline, category dots): These are read-top-to-bottom documents, not dashboards; Basecamp's document layout collapses to phones for free and keeps the print-sacred PublicProgram (with its exact 'New World School of the Arts' masthead) untouched.
- **Alert/notification philosophy (GlobalAlerts + any future digests)** ← Basecamp calm notifications: bundling, dot-vs-count badges, fixed reminder ladder, quiet hours: Safety-critical cancellations stay unmissable (strips on every page, all-clear state preserved) while everything else is bundled and quiet — the trust posture a school comms tool needs; no per-feature notification toggles.
- **Event detail page, desktop (/event/:id)** ← Airbnb listing detail: H1 + sectioned left column + ~372px sticky right action panel with bounded stickiness: The concert day sheet (call time, dress, venue, pickup) is this app's booking card, and 'Add to calendar' is its Reserve button — the single next action never scrolls off screen.
- **Event detail page, mobile** ← Airbnb sticky bottom action bar that replaces the global tab bar on detail screens: Date/venue left, 'Add to calendar' right — this one bar is what makes a long info page feel like an app on the phone where parents actually read it.
- **Long embedded lists (repertoire on hubs, rosters, linked pieces)** ← Airbnb progressive disclosure: show ~top-10 in-page + 'Show all 35' labeled-count button into a modal/subpage: Keeps hub pages short and scannable while guaranteeing completeness — the count in the label tells users nothing is hidden, unlike a carousel.
- **Campus map + event 'Where you'll be' section** ← Airbnb in-page map with click-to-activate scroll guard, exact pins, and pin↔row linking: The CampusMap/LocationText anchors already deep-link both ways; Airbnb's guard prevents scroll-hijack, and (inverse lesson from its radius-circle complaints) a school always shows exact pins.
- **Director ensemble page** ← Asana project header + multi-view tabs (List/Board/Calendar…) over one dataset: Overview / Roster / Attendance / Schedule / Repertoire / Seating / Program are all projections of one membership+event store already unified by rosterResolver.ts; fixed tabs (no user-created views) dodge Asana's documented clutter complaints.
- **Student & event records in the console** ← Asana slide-in task side pane carrying HubSpot 3-column record content (identity card + quick actions | timeline tabs | associations): A director triaging mid-rehearsal must open a student without losing roster scroll position; arrow-key-follows-selection and Esc-to-close come straight from Asana, the info/timeline/associations anatomy from HubSpot's contact record — structurally isomorphic to student/attendance/ensembles.
- **Director Today dashboard** ← Asana Home widget grid (fixed, not user-configurable) + QuickBooks Online 'Get things done' vs 'Overview' tab split: TodayView already has the right widgets (roll receipts, follow-ups, checklist); the fixed 2-column grid gives them desktop structure, and the QBO tab split separates 'run today' from 'season health' without building a report builder.
- **Director left navigation** ← QuickBooks Online grouped left rail: ≤6 accordion groups, collapse-to-icon-rail, click-not-hover: Solves the audit's worst nav defect — whosOut, scheduleChanges, and ensembleHub are unreachable from the menu today; grouping (Attendance/Schedule/People/Library) keeps every core object ≤2 clicks per QBO's own post-redesign lesson.
- **Universal create** ← QuickBooks Online '+ New' mega-menu (simplified to 2 columns), FAB + grouped sheet on mobile: A director posting a snow-day announcement or logging an absence should never navigate first; return-to-context on close is the load-bearing behavior, and the existing dir-drawer forms plug straight in.
- **Take Roll summary strip** ← QuickBooks Online money bar: status-segmented strip that doubles as a one-tap filter: 'Present 42 · Absent 5 · Late 3 · Unmarked 12' is both the glance metric and the fastest way to find unmarked rows in an 80-student list — cheap to build over the existing memoized StudentCard list.
- **Unmarked-roll and unexcused-absence follow-ups** ← QuickBooks Online banking review queue: count-badged 'For review / Done / Dismissed' tabs with inline-expanding row editor: Queue semantics give the follow-up workflow a goal state (empty = done) and an inline editor keeps corrections one tap deep; per QBO's auto-matching backlash, suggestions (e.g., planned-absence hints) pre-fill but never auto-commit.
- **Ensemble rows in both sidebars + 'what changed' awareness** ← Slack channel sidebar: sections, color-dot rows, unread bolding/dots (public side computed from a localStorage last-seen timestamp — no accounts): Ensembles-as-channels is a near-1:1 mapping and gives parents passive change awareness without login; per the Slack Activity criticism, every Updates-feed row leads with ensemble name + change type so it's decidable without clicking.
- **Global search / quick switcher** ← Slack Cmd+K quick switcher + persistent top-bar search field (Asana omnibox grouping): SearchOverlay and DirectorSearch already do grouped fuzzy results with keyboard nav; desktop just needs the always-visible field and the shortcut — type three letters of a student's name, jump to their record.
- **Piece page (/piece/:id) and PiecePicker program order** ← Spotify album/track detail: dominant-color gradient hero, numbered tracklist with durations, 'About' liner notes; program = ordered playlist: The one surface where the content is actually music: movements are literally a tracklist, program notes are liner notes, IMSLP/audio/video are play actions — Spotify's grammar fits without any of its utility failures (print output of the program stays sacred).
- **Ensemble 'artwork' system** ← Spotify dominant-color gradient theming, seeded from the existing ensembleColor hex (designed no-photo-first per Airbnb's card fallback rule): The data model has zero images; generated gradients + monogram glyphs give hubs and piece heroes visual identity that stays print-legible and never degrades into Spotify's identical-placeholder-tile problem.
- **Happening-now bar** ← Spotify persistent mini-player slot / Slack huddles docked bar, consolidating the existing NowNext banner + NowLine: 'Where am I supposed to be right now' is this app's now-playing; docking it above the tab bar (mobile) and viewport bottom (desktop) makes it ambient — but unlike Spotify it renders only when something is actually live, and yields to the SW update toast.
- **Roster / assignments / who's-out tables** ← HubSpot saved-view index table (view tabs, filter row, column gear, bulk-action bar) + QBO per-row primary verb with caret dropdown: 80+ row rosters need saved views ('All', per-ensemble, 'Seniors', 'Missing info'), inline edit, and bulk actions (graduate seniors, email parents); the per-row verb ('Open ▾ Log absence') is the density trick that keeps one-click actions without widening rows. Skip HubSpot's sharing/governance — one director.
- **First-run and empty states** ← HubSpot onboarding checklist + educational empty states (illustration + one sentence + primary CTA): SeasonChecklist already live-computes term setup; giving it HubSpot's persistent-checklist frame plus honest public empty states ('No upcoming events yet') costs little and covers the new-school-year cold start.

## 6. Phased build plan

- Phase 0 — Foundations, no visible change (M): unify the three token sets (retire/reclaim the dead 727-line index.css theme); px→rem migration OR architectural decision to mount new fixed chrome outside the .pub-app zoom subtree so the 'Aa' control survives; add a forced-light @media print token reset (fixes the latent dark-mode invisible-ink bug on all four print surfaces); formalize one z-index scale; write the contract-regression checklist (vanity slugs, hash anchors, query deep links, ICS URLs, SW cache, print outputs).
- Phase 1 — Public desktop shell (M): ≥1024px grid in PublicLayout.tsx with pinned left sidebar (contents = existing hamburger menu: primary five, ensemble color-dot rows, Resources, saved-student switcher), persistent header search field opening the existing SearchOverlay as a palette; mobile untouched; SW update toast slot preserved; sidebar hidden in print.
- Phase 2 — Airbnb detail patterns (M): PublicEvent two-column layout with sticky day-sheet action panel (desktop) and contextual bottom action bar replacing the tab bar (mobile); 'Show all N' progressive disclosure on hub repertoire/roster sections; click-to-activate map embeds with exact pins on event pages.
- Phase 3 — Ensemble hubs + Spotify moments (M): Basecamp tool-tile layout on PublicEnsemble under a generated color-gradient hero with Subscribe pill; PublicPiece tracklist/hero treatment; happening-now mini-bar consolidating NowNext (mobile above tab bar, desktop bottom strip), gated to only-when-live and print-hidden.
- Phase 4 — Director desktop shell (L): QBO three-zone frame in DirectorApp.tsx — grouped collapsible rail (surfacing whosOut/scheduleChanges/ensembleHub), thin top bar with save cue + Cmd+K, '+ New' create menu, gear settings split; mobile hamburger reorganized to mirror the same groups; DirNavigate intent deep links and WriteTray/FAB slots preserved; director dark tokens explicitly out of scope.
- Phase 5 — Director data surfaces (L): HubSpot saved-view tables for Roster and Assignments (view tabs, filters, column gear, bulk bar, per-row verb+caret); Asana slide-in record panes for Student and Event (full-screen sheets <768px); Schedule two-pane (month grid + sticky day panel); drawers become right slide-ins on desktop while remaining bottom sheets on mobile.
- Phase 6 — Roll & queue upgrades (M): QBO money-bar status strip on Take Roll (segments filter the list; chips on mobile); keyboard chords for desktop roll entry (arrows + A/L/E/S/Space, ? cheat-sheet overlay); review-queue framing (count-badged For-review/Done tabs) for unmarked rolls and unexcused follow-ups; optional compact-density toggle on tables.
- Phase 7 — Change-awareness layer (M): localStorage last-seen timestamps driving Slack-style bold/dot indicators on public ensemble rows; 'Updates' feed interleaving announcements + change notes with source-and-type-first rows and a 'since your last visit' divider; all-caught-up end states; verify EN/ES coverage for every new string added across phases.

Sizing is relative (S/M/L). Phases 1–3 are public-surface and additive; 4–6 are
the director console (ship over a school break, never mid-term); each phase is
revertible and gated by the Phase-0 regression checklist.

## 7. Roadblock register (six adversarial critics)

Six adversarial critics attacked the winning concept, each grounding claims in
the actual code. Verdict: **no unmitigable roadblocks** — the direction stands —
but 12 blocker-level findings (≈10 after dedup) require the spec amendments
below *before* implementation. 34 major findings need design changes inside
their phases; 54 "green light" findings confirm load-bearing decisions.

### Spec amendments adopted (from the blockers)

1. **The 4-tab bar is never removed on any public route.** The event action bar
   stacks *above* it (three lenses flagged the replacement rule independently;
   it deletes the only nav landmark on the exact page QR codes land on).
2. **Fix "Add to calendar" before promoting it.** The current `.ics` blob
   download silently fails non-technical Android users; route Android/desktop
   through a Google Calendar template URL (the SubscribeButton pattern), keep
   `.ics` for iOS, add visible success feedback, and wire the existing
   `misc.addToCalendar` ES translation.
3. **EN/ES is a per-phase exit criterion, not a Phase-7 sweep** — plus
   `t()` gains interpolation/plurals in Phase 0, dates become locale-aware, and
   pre-existing hardcoded English on the QR path is backfilled.
4. **The luminance-aware text-color function is a Phase-0 primitive** (no
   contrast machinery exists today — `ensembleColor()` returns raw hex), tested
   against both themes and every gradient stop, with a scrim fallback.
5. **Generated cover art gets an explicit `@media print` rendering mode**
   (outlined box, dark ink, no gradients) and "legible with background
   graphics off" becomes a print acceptance test; print blocks must force ink
   tokens, not just backgrounds, and reset the Aa zoom.
6. **Generated art never appears without the adjacent full ensemble name** —
   the real monograms (SO/CSO/CCO/OO/WE/CW/JE/HSC) are confusable at small
   sizes; the collapsed icon-rail drops ensemble monogram tiles; a monogram
   uniqueness pass is added. The instrument-glyph layer is cut from MVP (no
   data source exists).
7. **New fixed chrome requires solving the text-zoom problem first** — the
   "Aa" control CSS-zooms `.pub-app`; chrome inside it breaks at 1.3×, chrome
   outside it stops responding to the accessibility control. Resolution
   (rem-based Aa or explicit decision + compensations) is a Phase-0/1
   prerequisite, with a device test matrix at 1.15×/1.3× and a bottom-chrome
   pixel budget.
8. **Take Roll keeps the exception-only attendance model as a hard
   constraint.** The status strip derives Present (roster − exceptions), never
   writes it; "Space = present" is deleted from the keyboard spec; the
   write-guard becomes a per-student last-intent-wins queue.
9. **Status-segment filtering must never reflow the list under a pending
   tap** — marked rows hold position (grayed) until the filter is re-applied.
10. **`src/index.css` is extract-then-retire, not delete** — it carries the
    app's only global reset, `:focus-visible` outline, and the director-side
    reduced-motion kill switch; ~35 live lines move to a new `base.css` first.

### Plan amendments

- **Phase 0 grows** (and becomes the most important phase): `base.css`
  extraction; `t()` interpolation; luminance helper; SW build-hash injection
  (deploys currently never trigger the update toast) + lazy-chunk 404 recovery;
  a React-owned bottom-slot manager (the SW toast is imperative DOM outside
  React and its 76px anchor is already wrong at zoom); one shared `.no-print`
  utility replacing three hand-maintained print hide-lists; ESLint
  `no-restricted-imports` + bundle-size CI guarding the public/director
  code-split boundary.
- **Phase 1 is re-sized to L or split** (1a: pin the menu open as a sidebar;
  1b: zoom-subtree chrome extraction + header search).
- **Public desktop keeps the window as the scroll root** (inner-scroll wells
  break ScrollRestoration, hash anchors, and `?focus=` flows; director side may
  keep inner scroll).
- **Director-visible chrome batches into at most two term-boundary releases**;
  SW updates never auto-apply while a roll period is open.
- **Two pane primitives, not one**: a non-modal RecordPane (new Phase-0
  primitive; `useModalA11y` is a hard modal and its Escape handling breaks in
  layered UIs) and a modal FormPane with dirty-check — forms never render in
  RecordPanes.
- **Roster ships the HubSpot subset** (saved-view chips + bulk bar after
  explicit select mode; no hover-only reveals, no column gear).
- **The phone hamburger renders groups always-expanded, frequency-ordered**
  ("Take Roll ≤ 2 taps from anywhere" is a Phase-4 exit criterion); the FAB
  keeps contextual primary creates with the grouped sheet behind a caret.
- **Mini-bar rules**: suppressed on its own event's page; SW toast auto-yields
  after ~8s; dismissal is event-scoped (X only, no swipe gestures); no
  playback affordances anywhere (students' Spotify muscle memory).

### Full register by lens


#### Non-technical parent arriving via QR code

- **[BLOCKER] Event-page action bar replaces the tab bar on the exact page QR arrivals land on**  
  The base concept swaps the 4-tab bar for an Airbnb-style contextual action bar on /event/:id (mobile). The QR-flyer parent's path is /jazz -> ensemble hub -> tonight's concert -> /event/:id; for a cold visitor this is the first or second screen with app chrome they ever see, so global navigation vanishes before they ever learn it exists. For repeat visitors, chrome that disappears per-page destroys the spatial stability a 60+ occasional user depends on. The graft list itself calls this 'Desk & Door's one family-facing regression'. Compounding it: TextSize.tsx (verified) applies CSS zoom 1/1.15/1.3 to the whole .pub-app subtree, so any fixed bar mounted inside it grows ~30% at the Largest setting; tab bar (52px) + action bar + safe-area at 1.3x can eat 150px+ of a small Android viewport.  
  *Mitigation:* Adopt the graft as non-negotiable: stack the action bar ABOVE the retained tab bar, never replace it. Mount all fixed chrome outside the .pub-app zoom subtree, enforce the one-extra-occupant bottom-slot budget, add the short-viewport guard (hide the strip under ~560px-tall viewports), and device-test every fixed bar at Aa 1.3.
- **[BLOCKER] 'Add to calendar' is promoted to the Reserve-button primary CTA, but the mechanism is a silent .ics file download that fails non-technical Android users**  
  Verified in AddToCalendar.tsx: the button builds an .ics Blob and triggers an <a download> click. On a mid-range Android (Chrome or a QR scanner's in-app browser) that produces a quiet file-download notification; the user must find the file, choose an app, and confirm a Google Calendar import - with zero in-app success feedback (a documented Spotify-class H1 failure the audit explicitly warns about). A grandparent taps the biggest, pinned button on the page and nothing visible happens. The redesign amplifies this weak flow by pinning it on the sticky day-sheet card and the mobile action bar. Worse, the button text 'Add to my calendar' and its cancelled-event tooltip are hardcoded English (AddToCalendar.tsx line 148) even though translations.ts already has misc.addToCalendar with a Spanish value - the primary CTA ignores the language toggle today.  
  *Mitigation:* Before promoting the CTA, fix the flow: on Android/desktop route through a Google Calendar event-template URL (SubscribeButton.tsx already does platform detection and Google add-by-URL - reuse that pattern for single events), keep .ics for iOS, and show an in-app confirmation sheet ('Sent to your calendar - open Google Calendar to confirm'). Wire the existing misc.addToCalendar translation key. Treat 'tap CTA -> visible success' as an acceptance test on a real mid-range Android.
- **[BLOCKER] Spanish-speaking parents get an English redesign: the flagship new surfaces are unlocalized by design order, and all dates are hardcoded en-US**  
  Verified: every public date renders via toLocaleDateString('en-US', ...) (PublicHome.tsx:79, PublicCalendar.tsx, PublicEvent.tsx:48) - in Spanish mode a grandparent still reads 'Friday, December 12'. PublicEvent.tsx hardcodes safety-critical English: the cancellation banner sentence ('This concert is cancelled.'), the attendance-required paragraph, the ' - today' suffix, and the not-found message; SubscribeButton's 3-step wizard instructions are English-only. The redesign's most prominent new elements are all new strings - the action bar's leading label 'Fri Dec 12 · 7:00 PM', the happening-now bar ('NOW · ... · until 4:30'), 'Things to know', 'Arrive by / What to wear', 'Show all N', 'You're all caught up', 'Updates', 'since your last visit' - yet the concept defers 'verify EN/ES coverage for every new string' to Phase 7, AFTER Phases 2-3 ship the action bar and mini-bar. Net effect: the currently-translated day sheet gets wrapped in new English-first chrome - a regression for the exact persona the EN|ES toggle exists for.  
  *Mitigation:* Make EN/ES a per-phase exit criterion, not a Phase 7 sweep: no parent-facing string ships without a t() key, dates switch to locale-aware formatting keyed off nwsa.lang (a small wrapper around the existing parseDate/format helpers), and the pre-existing hardcoded strings on the QR path (PublicEvent banners, AddToCalendar, SubscribeButton steps) are backfilled in Phase 0 hygiene.
- **[MAJOR] QR landing (ensemble hub) leads with a 'Subscribe' pill and decoration; tonight's concert is the third tile down**  
  The redesigned hub hero (~140px gradient on mobile) contains name, member count, next-event line, and one pill action: 'Subscribe' (Spotify Follow framing). For a non-technical or 60+ parent, 'Subscribe' connotes payment or email spam, and it opens a multi-step calendar-feed wizard (with English-only instructions) - a scary flow promoted to the most prominent position on the very page paper-flyer QR codes land on. Meanwhile the mobile tile order is desktop order: Announcements, Upcoming rehearsals, THEN Concerts - so 'when is tonight's concert' sits below two other tiles plus the hero, and further below at Aa 1.3 zoom. A concert-day hero 'next-event line' that shows the 3:30 rehearsal instead of the 7:00 concert would compound it.  
  *Mitigation:* Make the hero's next-event line a full-width tappable row that deep-links to /event/:id, and on concert days pin 'Tonight: Concert · 7:00 PM · [venue]' above everything including the Subscribe pill; auto-promote the Concerts tile to first position on days with a concert; demote Subscribe to a secondary button with plainer wording ('Get these dates in your calendar').
- **[MAJOR] Gradient heroes are an entertainment affordance carrying real contrast and banner-blindness risk for the data they hold**  
  Ensemble hex colors are director-set arbitrary values rendered as Spotify-style gradient heroes with text (name, next-event line) on top - the audit itself flags WCAG failure risk, and Spotify's grey-on-grey chrome is a documented criticism. Separately, hero-like decorated bands are exactly what task-driven users skim past (banner blindness); putting the 'next event' fact inside the decoration invites a 60+ parent squinting in a parking lot to scroll straight past the answer. Nothing else in a schedule app teaches users that pretty banners contain load-bearing facts.  
  *Mitigation:* Make the grafted luminance-aware text-color function (with desaturation floor) a hard requirement of the same PR that introduces any gradient, not a follow-up; keep the gradient band decorative (name + monogram only) and render next-event/time/venue as plain high-contrast text on the card surface below it; snapshot-test hero text contrast across the full range of stored ensembleColor values.
- **[MAJOR] Bottom-edge choreography fails on concert night: the SW update toast outranks the happening-now bar, and dismissal is permanent**  
  The declared priority is SW update toast > happening-now mini-bar > event action bar, one occupant at a time. Concert night is precisely when a deploy's update toast is likely pending and when the parent most needs 'NOW · Concert · Auditorium' - under the stated contract the toast suppresses the live bar at peak moment. The mini-bar is also 'dismissible': one accidental swipe by a grandparent and 'where is my kid right now' is gone with no visible way to bring it back. And non-Spotify users have no model for a mini-player slot being tappable; a slim strip with a chevron is a weak affordance for the demographic.  
  *Mitigation:* During a live NOW window, auto-dismiss the SW toast after a few seconds and restore the mini-bar (the toast can re-offer on next navigation); make dismissal event-scoped (bar returns on the next NOW/NEXT change) and mirror the same information as a normal card at the top of Home and My Schedule so the bar is redundant, never the sole carrier; style it as an obvious button (label + chevron + pressed state).
- **[MINOR] QR scans frequently open in ephemeral in-app browsers where localStorage personalization silently does not exist**  
  Android camera/Google Lens and WhatsApp-forwarded links open in WebViews with isolated or ephemeral storage: saved-student identity, nwsa.lang, text size, and the new last-seen unread timestamps are all absent there. The redesign leans further on localStorage (unread dots, Updates divider, happening-now gating, sidebar student chips). Nothing on the core QR path strictly requires state - which must stay true.  
  *Mitigation:* Keep 'stateless QR arrival answers time/location/bring/add-to-calendar with zero saved state' as an explicit test case per phase; ensure unread indicators degrade to neutral (already promised) and never gate content; consider honoring the browser's Accept-Language for the default EN/ES choice on first visit.
- **[MINOR] Unread dots and an Updates feed import inbox anxiety into a schedule app for untrained occasional users**  
  Slack unread semantics train users that bold rows and dots demand clearing. A parent who opens the app twice a month gains little from device-local 'new since last visit' marks (often wrong on shared devices) and may read dots as unread obligations or, worse, trust a missing dot as 'nothing changed' when they simply used a different device. GlobalAlerts already carries everything safety-critical.  
  *Mitigation:* Keep dots menu/sidebar-only (never on the tab bar), dot-not-count, no 'mark all read' mechanics; frame the Updates view as a plain chronological 'Recent changes' list that is useful even with no last-seen timestamp; never let dot state alter content visibility.
- **[MINOR] The action bar's left half has an invisible tap behavior (scroll-to-day-sheet)**  
  In the mobile event action bar, tapping the date/venue text scrolls to the day sheet - a hidden gesture-like affordance of the kind the Spotify criticism catalog warns about. A grandparent tapping the date may expect a calendar and perceive a glitchy jump instead.  
  *Mitigation:* Either make the left half visibly a button ('Details ↓' chevron styling) or drop the scroll behavior and let only the labeled CTA be interactive; never make an unlabeled text block the trigger.
- **[MINOR] Desktop event page borrows Airbnb 'Share / Save' actions in an app with no accounts**  
  The desktop H1 row specifies right-aligned 'Share / Save'. Share is fine (URL copy), but Save implies a persisted favorites list that the no-login model cannot honor across devices; a parent who 'saves' an event on the school computer and looks for it on their phone finds nothing.  
  *Mitigation:* Drop Save, or redefine it as 'Add to calendar' (the app's real save verb), or scope it explicitly to the saved-student My Schedule mechanic with on-device wording.
- **[MINOR] 'What must my kid bring' still lives only in free-text notes below the fold**  
  The day sheet answers arrive/wear/venue/pickup, and the redesign's 'Things to know' grid covers Arrive by / What to wear / Pickup - but 'bring your instrument/music/black folder' remains buried in the Notes section under the day sheet on mobile. The redesign reorganizes the page without promoting the third question parents actually ask. Also verify the 3-column 'Things to know' grid linearizes to one column on narrow phones and at Aa 1.3.  
  *Mitigation:* Add a 'Bring' row to the day-sheet card (a short structured field directors fill alongside dress/pickup, falling back to a first-line notes preview), and specify single-column stacking for 'Things to know' below ~400px effective width.

  *Green lights:* The four public bottom tabs are frozen and identical across breakpoints (Slack isomorphism + Spotify tab-churn lesson) · Light, Basecamp-calm theme instead of Spotify dark chrome · No horizontal carousels anywhere and a bounded home ending in an explicit 'You're all caught up' row · GlobalAlerts cancellation/all-clear strips stay site-wide at the top of every page and are never demoted to the new unread mechanics (+6 more in the session record)

#### Student checking 30 seconds before rehearsal

- **[BLOCKER] Event-page action bar still specced to REPLACE the tab bar — a net regression on the page the mini-bar sends students to**  
  The winner's mobileLayout text says: on /event/:id 'the tab bar is swapped for a sticky bottom action bar' (Airbnb rule). Walk the student path: 30 seconds before rehearsal, tap the happening-now mini-bar to see pieces -> land on /event/:id -> Home/Calendar/My Schedule/Concerts tabs are GONE, replaced by a bar whose primary CTA is 'Add to calendar' — a parent action with zero value to a student already standing in the building. Today, PublicEvent keeps the tab bar (verified PublicLayout.tsx renders pub-tabbar on every route), so this is strictly worse than current. A graft ('stack the contextual action bar ABOVE the retained 4-tab bar') fixes it, but it lives only in the graft list while the winning spec text still contains the replacement rule — historically the kind of contradiction that ships wrong.  
  *Mitigation:* Edit the winning spec itself, not just the graft list: the 4-tab bar is never removed on any public route. The event action bar stacks above it under the one-occupant slot contract, and on mobile its primary text should be room + call time (tap scrolls to day sheet), with 'Add to calendar' secondary. Make 'tab bar present on every public route' an explicit exit criterion in the Phase 2 checklist.
- **[MAJOR] The only student-facing speed win (happening-now mini-bar) is demoted to an optional last phase — this persona gets nothing for six phases**  
  Count the current path: PWA cold-open lands on Home (GlobalAlerts = 0 taps for schedule changes), then 1 tap on My Schedule + ~1 screen of scroll past parent chips, hero, Subscribe, Planned absence, and announcements before the NowNext banner answers room/time (verified order in PublicSchedule.tsx:144-175). The redesign's genuine improvement — 0-tap room answer on ANY route via the mini-bar — is explicitly gated by the graft: 'named optional later phase... only after the slot contract exists and only if the restyled in-flow NowNext banner proves insufficient.' Meanwhile Phases 1-6 are desktop shell, director console, and tables: the 16-year-old's 30-second check is byte-for-byte identical (by design) through nearly the whole roadmap, and the concept's headline student benefit may never ship at all.  
  *Mitigation:* Pull two cheap student wins into the early quick-win release: (a) reorder /student/:id so NowNext (or a compact 'today strip') renders directly under the student hero, above Subscribe/PlannedAbsence/announcements — a JSX reorder, no new chrome; (b) commit to a measurable trigger for the mini-bar decision (e.g. if the reordered NowNext still requires a tap+scroll on the dominant entry route, build the bar in the next release) instead of an open-ended 'if insufficient'.
- **[MAJOR] The persistent SW update toast outranks the mini-bar at exactly the app-open moment**  
  The bottom-slot priority contract is 'SW update toast > happening-now mini-bar > event action bar', and the toast is the one element most likely to appear at cold open after any deploy. Verified in main.tsx:76-90: the toast has a single 'Refresh' action, no close button, and no auto-dismiss — under a one-occupant rule it suppresses the mini-bar for the entire session unless the student refreshes. The 30-second student opens the PWA and sees 'Update available - Refresh' where 'NOW - Rehearsal - Room 210' should be. School apps deploy in bursts (season start, concert week) — precisely the high-check-frequency windows.  
  *Mitigation:* Give the toast an auto-yield: show for ~8s then collapse to a small badge (or add a dismiss X), after which the mini-bar reclaims the slot. Alternatively invert priority when a NOW event is live for the saved student — a live room number is more urgent than a refresh prompt. Either way, the toast must never hold the slot indefinitely.
- **[MAJOR] Spotify mini-player muscle memory vs the happening-now bar: gestures, dismissal, and reappearance are undefined where students have the strongest trained habits**  
  The bar is deliberately placed in 'the Spotify mini-player slot' for an audience that uses Spotify daily. Their trained gestures: horizontal swipe = skip track, swipe up = expand, tap = expand player without losing context. The concept says only 'dismissible'. If dismissal is swipe-based, students will dismiss it accidentally while trying to 'see the next event'; if dismissal persists in localStorage for the day, the student who flicked it away at lunch has no bar at 3:29 pm — the exact failure the bar exists to prevent. Tap also does a route change (navigate to /event/:id) rather than Spotify's overlay-expand, so back-button expectations differ. Nothing specifies when a dismissed bar returns.  
  *Mitigation:* No swipe gestures on the bar at all (per the concept's own gesture-affordance criticism of Spotify). Dismiss = explicit small X only, scoped to the current event — the bar returns automatically for the next NOW/NEXT event. Tap navigates, so include a visible chevron and event title to signal 'this is a link, not a player'. Never add a progress bar, play triangle, or artwork square to it.
- **[MAJOR] Piece-page tracklist grammar promises playback the app cannot deliver**  
  The piece page copies Spotify's album anatomy: gradient hero, numbered tracklist with durations, action row. But the data model has zero per-movement audio (RepertoirePiece has only whole-piece imslpUrl/videoUrl/audioUrl external links — verified in PublicPiece.tsx:162-178 these open in new tabs). A student tapping movement rows that look exactly like Spotify tracks gets nothing; 'Listen' ejects them from the PWA into YouTube. Combine with a mini-player-shaped bar docked in the now-playing slot and the whole mental model says 'this app plays music'. Students will also read the starred 'My part' as a save/like toggle (Spotify heart) rather than what it is — an external PDF link (PublicPiece.tsx:134-137).  
  *Mitigation:* Style movements as a static numbered list: no row hover/press states, no per-row chevrons, no play iconography anywhere on the page. Label media actions with destination and external-link icons ('Listen on YouTube', 'Find on IMSLP') — the current implementation already does this; the redesign must not strip the labels for icon-only Spotify-style buttons. Render 'My part' as a labeled download/open button, not a star toggle.
- **[MAJOR] Mini-bar self-collision: on the event page it outranks the page's own action bar while linking to the page you are already on**  
  Priority is mini-bar > event action bar. The most common student journey during a live event is: tap mini-bar -> arrive at that event's /event/:id. Now the slot shows a bar that navigates to the current page (a dead tap), while the page's contextual action bar — the thing designed for this page — is suppressed. Spotify never shows the mini-player for the track whose page you have open in this way; it changes state. Here it is pure noise occupying the most valuable strip of a small phone.  
  *Mitigation:* Suppress the mini-bar when the current route is the event it points to (one route-param comparison), letting the event action bar take the slot. If the NOW event differs from the viewed event, keep the mini-bar — that case ('you are looking at Friday's concert but rehearsal starts in 5 minutes') is genuinely useful.
- **[MINOR] 'Show all N' progressive disclosure on event-page linked pieces regresses today's zero-tap program glance**  
  Verified: PubEventCard.tsx currently renders every linked piece untruncated, both on /student/:id today cards and on /event/:id. The Airbnb spec puts 'Linked pieces with Show all N' on the event detail page. 'What pieces are we rehearsing?' is one of the four 30-second questions; a rehearsal with 6-8 pieces now costs an extra tap plus a modal to answer 'is my piece on today?'.  
  *Mitigation:* Exempt event-page linked pieces (and My Schedule today-card pieces) from progressive disclosure — programs are short, bounded lists. Reserve 'Show all N' for genuinely long sets: hub repertoire, rosters, season lists.
- **[MINOR] Slack-style unread dots can train students to skip reading — and localStorage makes them lie**  
  Teenagers already fluent in badge semantics will learn 'no dot on my ensemble = nothing changed' faster than any other cohort. The last-seen timestamp lives in localStorage: cleared storage, incognito, a friend's phone, or a shared family iPad (parent checked at 2pm, wiping the 'new' state before the student looks) all produce a false 'nothing new'. The risks section says indicators 'degrade to neutral', but for a student a missing dot IS a neutral state that reads as an all-clear.  
  *Mitigation:* Dots are strictly additive attention hints: never pair them with any affirmative 'caught up' copy derived from last-seen data (the 'You're all caught up' end row must reflect feed completeness, not read-state). Schedule changes and cancellations always also render via GlobalAlerts and the red change banner regardless of dot state — keep that stated as an invariant in the spec, not just the risk list.
- **[MINOR] Assignment-due check still lives below Practice card and announcements on My Schedule**  
  The fourth 30-second question ('any assignment due?') currently requires scrolling /student/:id past Subscribe, PlannedAbsence, announcements, lesson chips, and the PracticeCard before 'Your assignments' appears (PublicSchedule.tsx:184-207). The redesign keeps My Schedule's aggregation but specifies no reordering, and the frozen 4-tab bar spends a tab on Concerts (low student frequency) while assignments remain a Resources-menu item. Status quo, not a regression — but the concept freezes it.  
  *Mitigation:* Within the My Schedule reorder (see the NowNext mitigation), add a compact due-soon line to the top cluster: 'Next due: Scales exam - Fri' linking to the assignments section. No nav change, no new route, keeps the tab contract frozen.
- **[MINOR] PracticeCard must not inherit playlist styling in the Spotify pass**  
  The concept maps 'Practice This Week = a personal playlist with check-offs'. The current card uses explicit labeled check buttons (PracticeCard.tsx:63-65). If the Phase 3 Spotify treatment restyles these rows as tracklist rows with circular toggles, Spotify semantics collide: a circled check on a track row means 'added/downloaded', and row-tap means play. Check-offs are also device-local, so any styling implying account-synced state ('liked songs') overpromises.  
  *Mitigation:* Keep the checkbox affordance visually a checkbox with its aria labels; keep row-tap inert or navigating to the piece, never toggling. Exclude PracticeCard from the tracklist component built for piece pages.

  *Green lights:* The happening-now mini-bar is the right idea for this exact lens: NowNext.tsx already computes current-or-next-event-today with room and time, but it renders on · Freezing the 4-tab bar (Home/Calendar/My Schedule/Concerts), keeping mobile Home order identical, and the 'mobile byte-for-byte unchanged' exit criterion protec · GlobalAlerts is never demoted: urgent/cancellation/all-clear strips stay site-wide at the top of content, cancelled events keep rendering struck-through instead · No horizontal carousels anywhere plus bounded sections ending in an explicit 'all caught up' row (+4 more in the session record)

#### Director mid-rehearsal (power user)

- **[BLOCKER] The Take Roll 'money bar' (Present / Unmarked segments) has no backing data model — the obvious implementation regresses live entry**  
  Attendance is exception-only: AttendanceStatus in src/director/types.ts is Absent/Late/Excused/Lesson, and useAttendance.ts stores NO record for present students — unmarked IS present by design (~5 writes per 60-kid rehearsal). The specced strip 'Present 42 · Absent 5 · Late 3 · Unmarked 12' is incoherent against this model: 'Present' and 'Unmarked' are the same set. If engineers resolve the ambiguity by adding explicit present marks (which the paired keyboard spec 'Space-present + Enter advance' outright requires), roll becomes 60 writes instead of 5, and toggleAttendance's settling guard ('if (studentId in optimistic) return') silently DROPS rapid inputs on the same student — e.g. correcting A→L within the echo window discards the correction with no feedback. Mid-rehearsal on school Wi-Fi that is corrupted attendance data, the one output this app must get right.  
  *Mitigation:* Keep the exception-only model as a hard constraint in the spec. Strip segments: 'On roster 62 · Absent 5 · Late 3 · Excused 2 · Lessons 1' with present derived (resolved − exceptions), never written. Delete 'Space-present' from the keyboard spec; keys mark exceptions only. Replace the drop-input guard with a per-student write queue (last intent wins) so rapid corrections are never silently ignored.
- **[BLOCKER] Tap-to-filter status segments over a live-reflowing roll list cause wrong-student mis-marks during entry**  
  The QBO money-bar segments 'tap-filter the list' (chips on phone). But the roll list mutates on every mark: filter to 'Unmarked', mark a kid Absent, and the optimistic recordMap update removes that row from the filtered set instantly — every row below shifts up under the director's thumb, and the next tap lands on the WRONG student's Absent button. QBO's money bar filters a static ledger; this list is the thing being edited. This is precisely the late-reflow mis-tap class the concept's own Spotify audit condemns, injected into the 20-times-a-day task, one-handed, on a phone, with 60 kids watching. A mis-marked absence then flows into the follow-up queue and parent-email flows (AbsenteeSummary tel:/mailto:).  
  *Mitigation:* Filtered rows must not reflow mid-entry: keep a just-marked row in place (grayed, with its new badge) until the filter is explicitly re-applied or the user scrolls; or make segments scroll-to-first-match instead of filter; or require an explicit 'Show only unmarked' toggle with rows collapsing via a 2-3s animated exit that never moves content under a pending tap. Pair every mark with the existing badge-pop + status badge so the director sees what was hit.
- **[MAJOR] Regrouping the phone hamburger to mirror the desktop rail adds depth to the highest-frequency navigation**  
  Today's phone menu (DirectorApp.tsx MENU_TABS) is 9 flat rows — hamburger → Take Roll is 2 taps. The concept reorganizes it into 6 groups + ensembles list + 4 gear items + public link + sign-out (~22 rows). If groups collapse like the desktop accordion, Take Roll becomes hamburger → Attendance → Take Roll (3 taps); if they render expanded, the menu becomes a 2-screen scroll where the daily items sit among Locations/ICS-import/QR-Kit setup rows. Either way the podium phone — the concept's own 'primary ergonomic target' — pays for a desktop symmetry rule. The Slack isomorphism rule is being applied to the wrong surface: parents need cross-device consistency; a single director needs speed.  
  *Mitigation:* On phone, render groups as always-expanded section headers (no accordions), order by frequency not by rail order (Today, Take Roll, Schedule, Who's Out first), and collapse the four gear/setup items behind one 'Setup' row. Treat 'Take Roll reachable in ≤2 taps from anywhere' as an explicit exit criterion for Phase 4.
- **[MAJOR] '+ New' grouped create menu adds a chooser step and a 10-option scan to creates that are 1 tap today**  
  On Schedule the FAB is new-event (ScheduleView.tsx dir-fab → EventForm, pre-filled with selectedDate); on Roster it is add-student. Converting the FAB into a universal grouped sheet (SCHEDULE/PEOPLE/COMMS/LIBRARY, ~10 targets) makes every in-context create 2 taps plus a visual search, and risks losing context defaults like defaultDate=selectedDate. The QBO '+ New' solves 'create from anywhere' — genuinely useful for posting an announcement from Take Roll — but the tax lands on the frequent case to serve the occasional one.  
  *Mitigation:* Contextual default: on Schedule the FAB's primary tap still opens EventForm with the selected date (and on Roster, StudentForm); the grouped sheet opens from a caret/long-press or from screens with no native create. In the grouped sheet, pin the current screen's contextual create as the first, largest target. Keep return-to-context on close as load-bearing.
- **[MAJOR] Session bar 'SO · 34/62 marked · Resume' misrepresents exception-based roll, and the bar-swap choreography invites bottom-edge mis-taps**  
  Because unmarked = present, a normal finished roll has ~5 marks out of 62 — a progress readout would show '5/62 marked' and scream 'barely started' for a roll that is done (roll receipts are stamped only when Finish is opened, TodayView already renders this ambiguity as 'Roll in progress — 5/62 marked'). Worse, the graft parks WriteTray undo/retry in the same consolidated bottom slot as the finish-roll bar with 'swap choreography': a 10s Undo toast (currently fixed z400 at the same edge as the sticky finish bar, writeTray.css) appearing or swapping exactly where 'Finish roll' sits means a director aiming at Finish can hit 'Undo delete' — a destructive control materializing under a committed tap.  
  *Mitigation:* Frame the bar by exceptions and receipt state ('Roll open · Symphony Orch · 5 exceptions · Finish'), never X/Y progress — or track an explicit 'roll finished' receipt as the only state signal. Bottom-slot contract must add a rule beyond one-occupant-at-a-time: no control may appear at, or swap into, a position within ~500ms of pointer-down, and Undo/Retry never occupies the Finish button's slot — stack the tray ABOVE the finish bar (current geometry) rather than replacing it.
- **[MAJOR] Status chips stack onto an already-tall sticky header, shrinking visible roster rows on the podium phone**  
  RollPeriod's sticky top (dir-roll-sticky-top) already holds back button + ensemble head + the dir-att-summary count line; below it stack the pulled-students strip, error strip, and section-gap warning. Adding a wrapping 6-chip status row as ANOTHER band costs ~40-80px, and with the ~56px app header the phone can end up with ~200px of chrome before the first StudentCard — maybe 4-5 visible students of 60 while scanning the room one-handed. The chips also duplicate the summary line's counts ('62 students · 5 exceptions · 1 at lessons'), violating the concept's own information-density goals on its most density-critical screen.  
  *Mitigation:* Merge, don't add: make the existing summary-line counts the tappable segments (bold count = filter affordance), keeping sticky-top height unchanged. If chips must exist, they replace the summary line rather than joining it, and the whole sticky top collapses to one 32px line on scroll-down.
- **[MAJOR] Non-modal slide-in panes with 'arrow keys move selection and the pane follows' create an unsaved-edit data-loss path and an a11y fork**  
  The spec applies one slide-in grammar to both READ panes (Student/Event record) and EDIT forms (EventForm 'and all managers open as right slide-ins'). If selection-follows-arrow-keys is live while a form pane holds unsaved edits — mid-triage, the exact 'director mid-rehearsal' scenario the pane is designed for — switching selection silently discards the edit, or worse re-binds the form to another record. EventForm also carries the concurrent-edit guard, which assumes one bound record per open form. Separately, every existing drawer is modal via useModalA11y (focus trap, Esc, focus return); a component that is non-modal on desktop and modal on mobile forks the focus-management contract and will regress keyboard users on one surface or the other.  
  *Mitigation:* Two named containers with different rules: RecordPane (read-mostly, non-modal, selection-follows, Esc closes) and FormPane (modal even as a slide-in: focus-trapped, selection navigation suspended, dirty-check confirm before close/switch). Never render EventForm or StudentForm in a RecordPane. Write this into the component spec before Phase 5.
- **[MAJOR] HubSpot table machinery on Roster is ceremony for one director and 94 students — and its hover-reveal contradicts the concept's own rules**  
  Today RosterView is: search box, ensemble chips, tap a row → StudentDetail (1 tap, zero learning). The redesign adds frozen checkbox column, saved-view tabs, filter dropdown row, column gear, bulk bar, per-row verb + caret dropdown, and 'row hover reveals Preview' — hover-only affordances are explicitly designed out elsewhere in this same concept per the Spotify audit, and don't exist on the touch devices the director actually uses. The mobile story compounds it: card linearization is governed 'per the column-priority order set by the desktop column gear', i.e., phone rendering depends on desktop-only, presumably device-local (localStorage) configuration a phone-first director will never set. Of the whole apparatus, only 'Missing info' and 'Seniors' views and a bulk bar are real gaps; per-ensemble views already exist as chips, and graduate-seniors already exists in SeasonChecklist.  
  *Mitigation:* Ship the HubSpot subset: saved-view chips (All / per-ensemble / Seniors / Missing info) + a bulk bar that appears only after a long-press/edit-mode toggle. Keep whole-row-tap → record pane as the single open affordance (no hover Preview, no caret menu). Cut the column gear; hard-code sensible columns per view and fixed card priority on mobile.
- **[MAJOR] Eight visible phases across a school year churn the PWA shell and podium muscle memory; Phase 4 bundles the phone menu change with the desktop shell**  
  Each visible phase implies an sw.js cache bump ('nwsa-hub-v1') and an update toast; the director's phone is a standalone PWA used offline-ish in rehearsal rooms — a stale/mixed shell or an update applied on navigation mid-roll interrupts the app's most critical 10 minutes. Phase 4 explicitly ships the reorganized phone hamburger together with the desktop rail work, so even a 'desktop' phase changes phone navigation; if any of Phases 4-6 slip past a semester boundary (L-sized phases slip), the director relearns chrome mid-term — the QBO force-migration lesson the risks section itself cites. The Start Guide (?staff=1 directors tab) also goes stale with each chrome change.  
  *Mitigation:* Batch director-visible chrome into at most two releases pinned to term boundaries (the additive early release: menu completeness, Cmd+K, receipts; and the shell release: rail + panes + tables together). Make 'no director chrome change mid-term' and 'SW update never auto-applies while a roll period is open' explicit release gates, and refresh Start Guide screenshots in the same PR as each shell change.
- **[MINOR] Keyboard roll chords: toggle semantics, Space/scroll conflict, and the S-opens-a-modal break — for a workflow that may never see a keyboard**  
  toggleAttendance toggles: pressing A twice on a row silently clears to present — a keyboard user double-tapping for confidence corrupts the record with no visual anchor. Space scrolls the page and activates focused buttons natively; 'S' (Lesson) opens LessonSheet, a modal that REQUIRES a typed reason (Save disabled until note.trim()), so one key in a rapid chord flow dead-stops into a form. The concept's own open question ('will roll ever realistically be taken on a keyboard device?') suggests this Phase 6 line item is speculative; roll is taken standing, on a phone.  
  *Mitigation:* If built: keys SET status idempotently (repeat = no-op), a distinct key (0/Backspace) clears, Space is preventDefault'd or unbound, and 'S' marks Lesson with the default window while the reason sheet queues for after Finish. Better: defer keyboard chords behind the device-reality open question and reinvest in phone roll polish (the concept's stated alternative).
- **[MINOR] One-group-open rail accordion taxes the roll ↔ who's-out ↔ schedule-change triangle**  
  The director's real loop crosses the proposed groups constantly: Take Roll (Attendance) → Who's Out (Attendance) → Schedule Change (Schedule) → back. With 'one group open at a time', every cross-group jump on desktop is expand-then-click. Clicks are cheaper than taps, but this is the 20-times-a-day path, and QBO's own rail navigates on group-header click precisely to soften this.  
  *Mitigation:* Allow multiple groups open (remembered), or make group headers navigate to the group's first destination on click while expanding. Keep cross-links inside views (the existing 'Subs & pull-outs' link in the roll summary, 'Who's out' buttons on event cards) as the primary triangle path so the rail is never required.
- **[MINOR] Rail count badges must be day-scoped and receipt-based or they become noise**  
  'Untaken rolls today' on the Take Roll rail item is only trustworthy if computed from today's rehearsal periods minus rollTaken receipts (the receipt is stamped only when the summary is opened — a director who marks absences but never taps Finish leaves a permanent phantom badge). A stale badge on daily-glance chrome trains the director to ignore badges, which then bleeds into ignoring the follow-up queue badge that actually matters.  
  *Mitigation:* Badge = today's Rehearsal/Sectional periods lacking a rollTaken receipt for that ensemble, clearing at end-of-day regardless; consider also stamping the receipt when exception count > 0 and the period is past its end time. Cap badges to the two queues with goal states (untaken rolls, unexcused follow-ups) and nothing else.

  *Green lights:* Surfacing whosOut, scheduleChanges, and ensembleHub as first-class destinations fixes a real, verified defect: DirectorApp.tsx has 12 VALID_TABS but only 9 MENU · The concept explicitly freezes the phone Take Roll core loop (large tap targets, sticky top summary, sticky finish bar, planned-absence accept-on-Excused, histo · The Asana slide-in record pane as the FIRST desktop investment is the right graft: StudentDetail/EventForm already render as self-contained drawer bodies, so th · Cmd+K + persistent search field is nearly free and high-value: DirectorSearch already ships grouped fuzzy results, full arrow/enter keyboard navigation, where-a (+6 more in the session record)

#### Accessibility & EN/ES internationalization

- **[BLOCKER] Gradient heroes from director-set arbitrary hex have zero contrast machinery behind them**  
  ensembleColor() (src/director/utils.ts:72-77) returns the raw stored hex with no validation, and a grep of src/ finds NO luminance/contrast utility anywhere — the 'existing --pub-gold-text correction' the concept says it will 'extend' is a single hand-tuned static token (--pub-gold-text: #7a5c00 at src/public/uiUpdates.css:311, dark variant #e4c968 at :348), not a mechanism. Even the fallback PALETTE fails white-text AA at normal sizes (#65a30d olive ~2.9:1, #db2777 ~3.9:1, #dc2626 ~4.0:1), and a director can set literally any hex. The hero design puts normal-size metadata (next-event line, member count, composer dates, duration) on the gradient, where 4.5:1 must hold at EVERY gradient stop. Worse, dark mode flips the ink token (--pub-ink #18212f -> #e7ecf2 via the prefers-color-scheme block at src/public/uiUpdates.css:338-358) while the data-driven gradient stays identical — a hero that passes in light mode can silently fail in dark mode, so contrast must be computed per-gradient, never inherited from the theme.  
  *Mitigation:* Build the grafted luminance-aware text-color function (WCAG relative luminance, desaturation floor, and a computed scrim/overlay when neither black nor white ink reaches 4.5:1) as a Phase-0 primitive inside the single deterministic cover-art component; unit-test it against all 8 palette colors, pure yellows/cyans, and both themes; optionally warn directors in EnsembleManager when a chosen color forces heavy correction.
- **[BLOCKER] CSS-zoom text-size control and the new fixed-chrome inventory are mutually hostile — and the proposed escape hatch quietly exempts the most critical text from the accessibility control**  
  Verified: TextSize.tsx (lines 17, 35-36) sets zoom 1/1.15/1.3 on .pub-app, and PublicLayout.tsx (lines 42-163) currently mounts EVERYTHING — header, menu, main, tab bar — inside .pub-app, so every proposed sticky element placed there scales. On a 360x640 phone at 1.3x: header 56->73px, tab bar (min-height 52px, uiUpdates.css:196-210) -> ~68px, happening-now bar 48->62px, and the grafted stacked event action bar ~64->83px = ~286px of chrome, under half the screen left for content. Meanwhile the SW update toast is appended to document.body OUTSIDE the zoom subtree with a hardcoded anchor bottom:calc(76px + safe-area) (src/main.tsx:82) computed in unzoomed pixels — at 1.15x/1.3x it lands ON TOP of the zoomed mini-bar/tab-bar stack instead of above it. But the graft's fix ('mount all new fixed chrome outside .pub-app') creates the inverse failure: sidebar labels, tab labels, the 'Add to calendar' CTA, and the happening-now room number — the exact strings a low-vision parent needs — would then NEVER respond to the Aa control at any setting.  
  *Mitigation:* Treat the px->rem migration (or an equivalent font-size-scaling implementation of Aa that also scales chrome typography) as a hard prerequisite for ANY new fixed chrome, not a deferrable alternative; re-anchor the SW toast to a CSS variable that reflects the real rendered bottom-chrome height; enforce the grafted device-test matrix (small phone, 1.15x and 1.3x, short-viewport guard <~560px) as a phase exit criterion with a bottom-chrome pixel budget.
- **[BLOCKER] Action-bar-replaces-tab-bar deletes the only primary navigation landmark exactly where QR codes land**  
  The current tab bar is the page's sole consistent nav landmark: <nav className="pub-tabbar" aria-label="Primary"> (PublicLayout.tsx:147). Desk & Door's mobile event-detail rule swaps it out for a contextual action bar on /event/:id — the single most common cold-entry page (printed QR posters and shared links land there). A screen-reader user navigating by landmark, or any first-time visitor, loses Home/Calendar/My Schedule/Concerts on the one page where they have no prior context; the back chevron assumes history that a QR scan doesn't have (it falls back, but 'back' is not 'navigation'). This is a net regression for the app's least-trained audience.  
  *Mitigation:* Adopt the already-grafted fix as a non-negotiable spec change: stack the action strip ABOVE the retained tab bar (the bottom:76px toast slot proves the geometry), keep the tab bar's aria-label="Primary", give the action strip its own labeled region (e.g., role="region" aria-label="Event actions"), and gate both behind the short-viewport guard so the pair never exceeds the bottom-chrome budget at 1.3x zoom.
- **[MAJOR] SearchOverlay is being promoted to flagship command palette while missing combobox semantics, focus return, and Spanish**  
  Verified in src/public/components/SearchOverlay.tsx: it declares role="dialog" aria-modal="true" (line 244) but does NOT use useModalA11y — there is no focus trap and no focus return to the opener on close (only autoFocus on the input plus a document-level Escape listener at lines 110-119). Arrow-key result selection is a purely visual data-active highlight (lines 226-240, 287) with no role="listbox"/"option", no aria-activedescendant, and no aria-live results count — a screen-reader user pressing ArrowDown hears nothing change and Enter activates an invisible selection. Group labels ('Events', 'Repertoire'...), the placeholder 'Search events, music, announcements…', the hint, and 'No matches for' are hardcoded English (lines 143-273) in an app whose parent audience is the reason EN/ES exists. Anchoring this same component into a persistent desktop header field and a Cmd+K shortcut makes these defects the centerpiece interaction.  
  *Mitigation:* Before the palette ships: convert to the ARIA combobox/listbox pattern (aria-activedescendant, aria-expanded, options with role=option, polite results-count announcement), return focus to the header field/opener on close, key every string through t() with ES values, and reconcile the desktop anchored variant's semantics (non-modal popup, not aria-modal dialog).
- **[MAJOR] The Asana-style non-modal slide-in pane cannot be built on useModalA11y, and stacked Escape handling is already broken at document level**  
  The pane spec requires 'list remains interactive; arrow keys move selection beneath and the pane follows; Esc closes' — but the only focus-management primitive in the codebase, useModalA11y (src/shared/useModalA11y.ts), is a hard modal: it traps Tab inside the panel (lines 31-43), which would make the supposedly-interactive list unreachable by keyboard. Its Escape handler (lines 26-30) calls e.stopPropagation() on a document-level keydown listener — stopPropagation does NOT stop other listeners on the same node, and SearchOverlay registers its own document-level Escape (SearchOverlay.tsx:111). Today this is masked because overlays are mutually exclusive full-screen modals; in the new layered world (persistent record pane + command palette + menu simultaneously open) one Esc press closes every layer at once, and the focus-return chain (prev?.focus at useModalA11y.ts:48) restores focus to elements that may no longer reflect the selection the pane was following.  
  *Mitigation:* Design and build a second, explicitly non-modal pane primitive in Phase 0: no Tab trap; Esc scoped via keydown on the pane subtree (or a top-of-stack layer manager with stopImmediatePropagation semantics); roving tabindex + aria-selected on the list with the pane exposed as a labeled complementary region; a documented focus contract for open (focus stays in list) vs. explicit enter-pane (F6-style or a 'Details' button); URL updates announced politely.
- **[MAJOR] t() has no interpolation or plurals, but the redesign's signature strings are all parameterized**  
  Verified: t(key) in src/shared/i18n.ts:52-56 is a static dictionary lookup over ~107 keys (src/shared/translations.ts) — no variable substitution, no plural rules. The concept's load-bearing new strings are inherently parameterized: 'Show all 35', 'Students · 94', 'SO · 34/62 marked · Resume', 'since your last visit', 'NOW · Symphony Orchestra rehearsal · Room 210 · until 4:30', 'Present 42 · Absent 5 · Unmarked 12'. Without t(key, params), every one of these gets built by string concatenation in EN word order, which produces broken Spanish ('Mostrar todos 35' vs 'Ver los 35' ordering problems, 'hasta las 4:30' vs 'until 4:30' placement) or forces devs to skip i18n entirely — and the director console currently has ZERO Spanish, so the entire new Desk chrome (rail groups, '+ New' menu, saved-view names, money-bar segments) lands English-only by default.  
  *Mitigation:* Extend i18n.ts with t(key, params) interpolation and a minimal count/plural helper in Phase 0 (it is ~20 lines and preserves the en->key fallback); make 'every new user-facing string has an EN and ES key' a phase exit criterion (Phase 7 already gestures at this — move it to every phase); explicitly decide the director-console Spanish question before Phase 4 rather than inheriting English by accident.
- **[MAJOR] Spanish expansion breaks the specific fixed-width slots the redesign introduces**  
  Concrete collisions from src/shared/translations.ts: 'misc.addToCalendar' es = 'Agregar a mi calendario' (23 chars vs 18, ~+28%) is the right-side primary button of the mobile event action bar that shares one 360px-wide row with a date+venue left stack — at 16px bold plus padding the button alone approaches ~220px, truncating the venue (the information half of the bar). The happening-now strip's NOW/NEXT badges become AHORA/PRÓXIMO (+67-75%) inside a single 48px line that must also fit ensemble name, room, time, and a dismiss control on 320-360px phones — and the room number, the actual payload, is what ellipsis eats first. 'nav.concerts' es 'Temporada de conciertos' (23 vs 14 chars) fits the 264px sidebar row but leaves no headroom at larger font settings; the Take Roll money-bar segments (Presente/Ausente/Tarde/Justificado/Lección + counts) and saved-view chips (e.g., 'Missing info' -> 'Información incompleta') will wrap or scroll on phones. The tab bar itself is fine (existing concertsShort/assignmentsShort short-key pattern proves the mitigation).  
  *Mitigation:* Extend the existing *Short key convention to every new chrome string (action-bar CTA, badges, segment labels); design all fixed bars mobile-first AT Spanish + 1.3x as the sizing case, not English at 1x; define per-slot truncation priorities that never drop room/time; add an ES-locale visual regression pass to the phase checklist.
- **[MAJOR] Roster spec reintroduces hover-only affordances the concept itself bans**  
  The desktop Roster spec says 'row hover reveals Preview (opens side pane) and per-row primary verb + caret' — a hover-revealed control is invisible to keyboard users, nonexistent on touch/iPad (the codebase deliberately gates hover behind @media (hover: hover) in src/public/uiUpdates.css:368 for exactly this reason), and is literally item three of the Spotify anti-pattern list this concept claims to design out ('hover-only affordances'). A director on an iPad at the podium — the concept's own open question — never discovers Preview.  
  *Mitigation:* Make row actions revealed on hover AND :focus-within, and always visible on coarse pointers ((hover: none)); guarantee every hover-revealed action has a permanent path (row tap opens the pane; verbs repeated inside it); include keyboard and touch walkthroughs of the table in the Phase 5 acceptance tests.
- **[MAJOR] No landmark architecture exists for the sidebar + main + persistent-bar layout, and the happening-now bar has no screen-reader story**  
  Today's structure is minimal and clean: header, <main className="pub-content"> (PublicLayout.tsx:140), one <nav aria-label="Primary"> tab bar (line 147), with GlobalAlerts correctly inside main using role="status" aria-live="polite" (GlobalAlerts.tsx:52,59). The redesign multiplies nav-shaped regions — desktop sidebar (3 sections + ensemble list + saved students), header search, mobile tab bar, Resources menu, happening-now bar, sticky day-sheet panel — with no specified landmark roles or accessible names; duplicate unlabeled <nav> elements are a WCAG/ARIA landmark-discoverability failure. The happening-now bar is dynamic chrome: its data source re-renders on a 30-second interval (setInterval in src/public/components/NowNext.tsx:15) and the bar 'renders only when live' — without a role and polite announcement SR users never learn it exists; with naive aria-live plus a countdown it announces every tick. The sticky day-sheet also duplicates date/time/venue that already appear in the body (and a third time in the mobile bar), tripling SR reading order.  
  *Mitigation:* Specify the landmark map in Phase 1 as a deliverable: distinct aria-labels per nav region (e.g., 'Primary', 'Ensembles', 'Resources'), sidebar as one <nav> with headed groups, happening-now as a labeled region/complementary landmark whose APPEARANCE is announced once politely and whose countdown text is aria-hidden or minute-granular, and the day-sheet panel as an aside whose duplicated fields reference (not repeat) the canonical body content.
- **[MAJOR] Print protection is three hand-maintained hide-lists that new chrome will silently miss — and dark-mode ink plus text-size zoom already leak into print**  
  Verified: the three public print blocks each independently enumerate chrome to hide — public.css:781-787, season.css:74-84, startGuide.css:80-97 all list .pub-header/.pub-tabbar/alert strips (qrKit.css:72-111 is safe via its body:has() visibility scoping). None of them will hide a new sidebar, header search field, happening-now bar, action strip, or session bar unless every one of the three lists is edited per element — a fridge copy printing with a sidebar down its edge is one forgotten selector away. They also force only BACKGROUNDS white; body text keeps var(--pub-text)/var(--pub-ink), which the dark-mode block flips to #e7ecf2 (uiUpdates.css:342-344) — in browsers that honor OS dark scheme when printing (Firefox), the concert program's piece details and program notes print as near-invisible ink today (only .pub-program-piece-title is forced #000 at public.css:786). Additionally, no print block resets the Aa zoom, so a family with 'Largest' set prints a 1.3x-scaled program with different pagination.  
  *Mitigation:* The concept's Phase-0 forced-light print token reset is the right move — make it also (a) force ink tokens, not just backgrounds, (b) add '.pub-app { zoom: 1 !important }' under @media print, and (c) consolidate chrome-hiding: mount all persistent chrome under one wrapper hidden by a single shared print rule, with a four-surface print smoke test (program, season, start guide, QR kit, in light AND dark OS mode) in the release checklist.
- **[MINOR] Spanish content renders without lang="es", and the new Updates feed will spread the defect**  
  PubAnnouncements.tsx (lines 28-31) swaps in titleEs/bodyEs for the per-announcement ES flip without setting lang="es" on the swapped nodes; i18n.ts stamps only the document-level <html lang> (lines 60-62). A screen reader in EN mode reads Spanish announcement text with English pronunciation rules (and vice versa). The proposed Updates feed interleaves announcement content on every visit, multiplying exposure.  
  *Mitigation:* Add lang={showEs ? 'es' : undefined} to per-item translated content now (one-line fix) and adopt it as a rule for the Updates feed rows and any per-item translated string.
- **[MINOR] Single-character roll-marking shortcuts need WCAG 2.1.4 scoping**  
  Phase 6's A/L/E/S/Space/Enter chords are single-character key shortcuts; WCAG 2.1.4 requires them to be remappable, disableable, or active only when the relevant component has focus — a global document listener would also collide with the planned Cmd+K field and DirectorSearch typing, and Space doubles as page scroll when focus is ambiguous.  
  *Mitigation:* Scope marking keys to the focused roll row via roving tabindex (which satisfies the 2.1.4 focus exception and matches the pane's arrow-key model), never bind them at document level, and document them in the '?' cheat-sheet overlay.
- **[MINOR] Happening-now bar packs a full-bar link plus a dismiss button into one 48px strip**  
  A dismissible slim strip where the entire bar is a link means the dismiss control sits flush against a large adjacent target in the thumb zone directly above the tab bar; at less than 44px with no spacing it invites mis-taps that either navigate when dismissing or dismiss when navigating (WCAG 2.5.8 minimum 24px with spacing; the app's own established floor is 40-52px, e.g., .pub-tabbar-btn min-height 52px).  
  *Mitigation:* Give the dismiss control a >=44px hit area visually separated from the link region, make dismissal reversible (it reappears on next NOW/NEXT change), and verify the geometry at 1.3x text size where the strip scales.

  *Green lights:* useModalA11y (src/shared/useModalA11y.ts) is a genuinely solid MODAL primitive · Two global prefers-reduced-motion kill-switches already exist (src/public/uiUpdates.css:405-412 zeroing all animation/transition durations, plus src/index.css:7 · GlobalAlerts already uses role="status" aria-live="polite" with a distinct all-clear state (GlobalAlerts.tsx:52,59), and the concept explicitly keeps the strips · The i18n store degrades safely by design: t() falls back es->en->key (src/shared/i18n.ts:52-56), <html lang> is stamped before first paint (lines 60-62), and us (+4 more in the session record)

#### Content & artwork reality (no images, no accounts)

- **[BLOCKER] Generative cover art is invisible ink on the load-bearing print surfaces**  
  The cover-art spec (two-tone CSS gradient + light monogram) fails in print by default: browsers strip background-color and background-image unless print-color-adjust:exact is set, and the app's existing print CSS forces backgrounds white but NOT text colors (documented latent bug in the audit). Any tile or hero whose monogram/title is a computed light color over a gradient prints as white-on-white — literally blank boxes — on the four physical surfaces (concert program, Season fridge copy, Start Guide, QR kit posters/slips, qrKit.css uses inch/pt units and per-ensemble accent colors). The graft note says 'gradients must stay print-legible on QR posters' but names no mechanism; the Phase 0 forced-light token reset fixes tokens, not white text baked into an art component. A family fridge copy or QR poster that prints with unreadable ensemble identity is a hard regression versus today's flat accent bars.  
  *Mitigation:* Ship the cover-art component with an explicit @media print rendering mode: no gradient, 1px outlined box or solid-ink accent bar, dark monogram text, pt-based sizing. Make 'renders legibly with background graphics disabled' an acceptance test for every print surface, gated by the Phase 0 regression checklist. Never let the component's screen variant reach a print stylesheet.
- **[BLOCKER] Monograms are confusable across NWSA's real ensemble names — fatal wherever art appears without the full name**  
  Verified against seedData.ts and EventChip.tsx ensembleMonogram(): the eight ensembles yield SO, WE, CSO, JE, CW, CCO, HSC, OO. Five of eight monograms are permutations of C/S/O/W — 'CSO' (Camerata String Orchestra) vs 'CCO' (College Chamber Orchestra) vs 'SO' (Symphony Orchestra) vs 'OO' (Opera Orchestra) differ by one glyph at 40-48px. The design has at least one surface where art stands alone: the director rail 'collapsible to 64px icon rail' would render ensemble rows as monogram tiles only. A director opening CCO's roll believing it is CSO's marks attendance against the wrong ensemble — a data-integrity regression, not cosmetic. This is exactly Spotify's identical-placeholder-tile failure reappearing through the text channel instead of the image channel: with near-identical hues (see the color roadblock) AND near-identical monograms, the tiles carry no independent scent.  
  *Mitigation:* Hard rule: generated art never appears without the full ensemble name adjacent (tiles are text-first with art as accent; collapsed icon rail shows click-opened flyout labels or is dropped for the ensembles section). Add a uniqueness pass to monogram generation (avatar-system style disambiguation) and a design-time collision report so a new 'Chamber Choir' cannot silently mint a second 'CC'.
- **[MAJOR] The 'instrument glyph' layer has no data source and no differentiating power**  
  Verified in src/director/types.ts: Ensemble is {name, order, color?, defaultLocation?, defaultStartTime?, defaultEndTime?, meetingDays?} — there is no instrument, family, or icon field anywhere in the data model. The Encore graft's 'monogram + instrument glyph' must therefore infer glyphs from name substrings (the same fragile trick vanity.ts uses) or add a new director-maintained field. Worse, five of the eight real ensembles are orchestras (Symphony, Camerata String, College Chamber, Opera, plus Chamber Winds); an honest inference gives most tiles the same orchestra/strings glyph, adding visual noise with near-zero scent. Name inference also breaks the moment the director creates 'Guitar Ensemble' or 'Percussion Ensemble' with no mapping — the system silently degrades for exactly the ensembles that most need identity.  
  *Mitigation:* Cut the glyph from the MVP cover-art spec; ship gradient + monogram + mandatory adjacent name. If glyphs are wanted later, add an optional icon picker to EnsembleManager (one-time director choice, safe default of none) rather than name inference, and only render the glyph at 128px and above.
- **[MAJOR] The piece 'album' page is designed for metadata the data model does not guarantee**  
  Verified in types.ts: RepertoirePiece requires only {id, ensembleId, title, order}. Composer, composerDates, duration, movements[], programNotes, instrumentation, imslpUrl/videoUrl/audioUrl, and partsLinks are ALL optional, and PieceMovement.duration is optional even when movements exist. Most wind/jazz charts are single-movement, so the signature 'numbered tracklist table (# / title / duration)' renders as one row or zero rows for a large share of the library; program notes are realistically written at concert time, if ever. A 32-48px-title gradient hero plus 'About' block wrapped around two known facts (title, ensemble) is Spotify cosplay on an empty record — the page reads as broken rather than minimal, and it buries the data that always exists and that families actually come for (my part link, programmed-for concerts, listen links).  
  *Mitigation:* Make every album-grammar section strictly conditional (no empty tracklist header, no empty About), scale the hero down when metadata is sparse, and define the page spine as the always-present utility data: ensemble, programmed-for concerts, per-instrument parts, external links. Treat the tracklist as an enhancement earned by multi-movement works, not the page's identity.
- **[MAJOR] Every piece in an ensemble gets identical 'cover art' — the system must be fenced off from piece tiles**  
  Pieces carry a single ensembleId and no imagery, so a hero 'from the owning ensemble's color' means all ~30 Symphony Orchestra pieces share one indistinguishable blue gradient. That is harmless on the piece page itself, but the graft language ('the shared art system for heroes, tiles, and sidebar rows') invites tile/grid treatments of repertoire, which would reproduce the audited Spotify failure verbatim: an artwork-first grid of identical rectangles where the text — the only real scent — is the smallest element. Hashing hues from piece titles is not an out either: data-driven ensemble colors are a school-wide semantic language (calendar dots, event bars, print accents, per audit constraints), and per-piece colors would pollute it.  
  *Mitigation:* Write the scope rule into the component spec: generated art applies to ensembles, concert date-blocks, and assignment tiles only. Pieces are always text rows / track tables (the concept's own repertoire treatment), never tiles, at every size, in every future phase.
- **[MAJOR] Color is the gradient system's only variable, and it is weaker than assumed**  
  Verified in src/director/utils.ts: the fallback PALETTE has exactly 8 hues and ensembleColor() assigns by (order-1) % 8 — a 9th ensemble silently duplicates hue #1. Director-set hex is unvalidated, so an ensemble can be set to concert gold #ca8a04 or assignment violet #7c3aed, breaking the reserved semantic colors the palette comment explicitly protects ('so an ensemble's dots can never be mistaken for either'). For the ~8% of color-blind users, red #dc2626 / olive #65a30d / green #16a34a / orange #ea580c collapse toward two hues, and two-tone gradients derived from one hex have identical structure tile-to-tile — so at grid scale the ensembles page is, for a meaningful user slice, eight same-shaped rectangles distinguished only by confusable monograms (see blocker above). The existing EventChip already knows this lesson ('shape ALWAYS paired with the type word, never color/shape alone'); the new tiles must inherit it.  
  *Mitigation:* Enforce it in data and design: EnsembleManager validates custom hex against reserved gold/violet and against other ensembles' colors (minimum perceptual distance); the cover-art component varies a secondary channel (gradient angle or second-stop hue rotation seeded from the ensemble id) for structural differentiation; tiles keep the full name as the largest element per the stated text-primary rule.
- **[MAJOR] Basecamp tool-tile hubs presume a content cadence a school ensemble does not have**  
  Basecamp hubs work because teams generate daily messages and todos; an NWSA ensemble generates a static weekly rehearsal pattern, 3-4 concerts a year, sporadic announcements, and seating charts that are optional and may never be published. Of the six specced tiles (Announcements, Upcoming rehearsals, Concerts, Repertoire, Seating charts, Roster), two to three will show empty previews for most of the school year on most ensembles — eight hubs of half-empty tiles read as institutional and abandoned, the 'ghost town' failure mode. The obvious fix, hiding empty tiles, breaks the cross-ensemble spatial consistency that is the whole point of a fixed hub layout (Jazz shows six tiles, Opera three, and parents with kids in both lose the muscle memory the concept promises).  
  *Mitigation:* Keep the tile set and order fixed across all ensembles, but design honest, dated empty states ('No announcements — last posted Oct 2') and demote features an ensemble has never used (seating never published) to a quiet single-line link row below the grid rather than a full empty tile. Measure the hub against its real job: 'next rehearsal' and 'subscribe' must be visible without scrolling on a phone.
- **[MAJOR] The ensemble hero band has nothing to be made of — and no field to fill it with**  
  Airbnb and Spotify heroes earn their height with information-bearing imagery (what the listing looks like, recognizable album art). The Ensemble entity has no description, blurb, photo, or tagline field, so the specced ~220px desktop hero can only contain the name, a computed next-event line, a member count, and a Subscribe pill over decoration — a gradient restating what a 10px color dot already says. If the school is expected to author blurbs and photos to justify the band, that is a new, permanently sustained content program for one director across eight ensembles (the concept's own open question concedes nobody has confirmed such content exists); if not, the hero is permanent brand air that pushes the actual answer down on every visit. The audited Spotify criticism 'low information density for scanning' lands squarely here.  
  *Mitigation:* Cap the hero by its live content, not a fixed 220px: name + next-event + subscribe fits in ~120-140px on desktop too. Do not add a blurb field or photo slot to the data model as a design dependency; if a description field is ever added, the hero grows only when it is populated. State explicitly that the gradient system is the permanent design (the context's own conclusion), so no phase is allowed to block on photography that will never arrive.
- **[MINOR] The 40/48px sizes cannot hold the full art spec**  
  At 40px, a two-tone gradient + 2-3 letter monogram + instrument glyph is three symbolic layers in a thumbnail; 'CSO' alone at 40px is roughly 10px per glyph, at the floor of legibility, and adding a glyph guarantees clutter. The component spec lists four sizes but no per-size content rules.  
  *Mitigation:* Define size-adaptive content in the component contract: 40/48px = gradient + monogram only (or plain color dot, which the sidebar spec already uses and which is cheaper and proven); 128px adds the glyph if it survives the glyph roadblock; 232px adds secondary text. Sidebars keep the existing 10px dot + name rows — do not upgrade them to art tiles.
- **[MINOR] Gradient text contrast must be computed against both stops, in dark mode, at Aa zoom**  
  The grafted luminance-aware text-color function is the right instrument, but a two-tone gradient means the title crosses two luminances plus their midpoint; the public surface also has an automatic prefers-color-scheme dark variant and a CSS-zoom text-size control (1.3x) that scales tiles and can wrap or clip monograms. Concert-gold tiles specifically inherit the known gold-contrast problem the codebase already patched once (--pub-gold-text darkened to #7a5c00).  
  *Mitigation:* Contrast-test the computed text color against the worst gradient stop, not the base hex; include dark-token and zoom-1.3 renders of the cover-art component in its unit/visual tests; reuse the existing gold text correction on all gold date-block tiles.

  *Green lights:* The concept is honest about its imagery reality: the data model has zero images (verified · Deterministic, single-component derivation from ensembleColor directly answers Spotify's audited layout-instability criticism: the same ensemble renders identic · The stated 'text stays primary, art secondary' rule has strong existing DNA in the codebase: EventChip already pairs monogram + tint + shape + type word and doc · The concert gold date-block tile is the one tile in the system that carries genuine information (the date itself), and the reserved gold/violet semantics are al (+4 more in the session record)

#### Engineering migration realism

- **[BLOCKER] Deleting the 'dead' 727-line src/index.css as planned breaks every page — it is the app's only global reset and a11y base**  
  Verified: the component classes (.app, .bottom-nav, .exercise-*, .btn-*, ~650 lines) have zero references in any TSX file and are safely dead. But the same file carries live, load-bearing globals the whole app depends on: (1) the ONLY universal reset '*{box-sizing:border-box;margin:0;padding:0}' — public.css has no reset of its own, so deleting it un-zeroes h1/p/ul margins on every public page; (2) '#root{min-height:100dvh;display:flex;flex-direction:column}' which the .pub-app shell sits inside; (3) 'html{font-size:16px;scroll-behavior:smooth}' and 'body{background:var(--bg)...}'; (4) the app-wide ':focus-visible{outline:2px solid var(--purple-l)}' — deleting the :root tokens makes the var() invalid and focus outlines silently VANISH app-wide, an a11y regression no type-checker or screenshot diff of happy paths will catch; (5) the director surface's only prefers-reduced-motion kill switch (public has its own duplicate at public/uiUpdates.css:405, director does not); (6) scrollbar styling. The Phase 0 / 'Same Stage' graft language ('deletion of the dead 727-line index.css theme' inside a no-visible-change release) is unshippable as written.  
  *Mitigation:* Reframe as extract-then-retire: move ~35 live lines (reset, html/body/#root, scrollbar, :focus-visible recolored to a brand token, reduced-motion) into a new base.css imported first in main.tsx; then delete only class rules individually verified dead by grep; keep a before/after visual diff of public+director+all four print surfaces as the exit gate.
- **[MAJOR] The service worker will not announce most phase deploys, and open PWAs can 404 on the lazy director chunk mid-migration**  
  public/sw.js precaches only BASE/ and manifest.json with a static cache name 'nwsa-hub-v1' and no build fingerprint. A deploy that changes only app assets leaves sw.js byte-identical, so 'updatefound' never fires and the update toast in src/main.tsx NEVER shows — the concept's assumption that the toast mediates each phase transition is false for typical deploys. Worse, sw.js calls skipWaiting()+clients.claim(), so when sw.js IS edited the new SW seizes open tabs immediately while they run old HTML; and because hashed chunks are cache-first but only cached after first use, an open tab from before a deploy that lazily loads the DirectorApp chunk (or any future code-split page) fetches an old-hash URL that GitHub Pages no longer serves → 404 → AppError. Eight phased deploys multiply this exposure window; 'atomic cache-name bumps per phase' as a manual convention will be forgotten.  
  *Mitigation:* Automate: inject a build hash into sw.js (cache name) at build time so every deploy triggers updatefound and the toast; precache the shell's current hashed entry assets at install so old tabs stay coherent; add a dynamic-import failure handler (catch chunk-load error → prompt/perform one reload) in main.tsx/AppError. Make this part of Phase 0, before any visible phase ships.
- **[MAJOR] The bottom-slot stack contract cannot be implemented in CSS alone — the SW toast is imperative DOM outside React**  
  The concept's collision engine (SW update toast > happening-now mini-bar > event action bar > tab bar) assumes coordinated occupants. Reality: the toast is a raw div appended to document.body from main.tsx with hardcoded 'bottom:calc(76px + safe-area)' and z-index 9999 — outside React, outside the router, and outside the .pub-app zoom subtree. The tab bar is position:sticky IN-FLOW inside the zoomed subtree (52px, rendering ~68px at Aa 1.3x — the 76px constant is already marginal at max zoom). The planned mini-bar and mobile event action bar are React components. 'Toast wins' priority requires either porting the toast into React state or a documented DOM protocol (e.g., a CSS variable for current bottom-chrome height + MutationObserver), none of which is in any phase.  
  *Mitigation:* Build the slot-stack as code in Phase 0/1: move toast rendering into a React BottomSlot manager (or expose --bottom-chrome-height on :root that both worlds honor), and add a device-matrix test at zoom 1, 1.15, 1.3 with safe-area insets before Phases 2/3 ship any new bottom chrome.
- **[MAJOR] Public desktop grid must keep the window as the scroller or it silently breaks ScrollRestoration, anchors, and swipe**  
  main.tsx uses react-router's <ScrollRestoration/>, which restores WINDOW scroll only. Hash/query deep-link behaviors (/map#anchor via CampusMap.tsx scrollIntoView, /ensemble/:id#repertoire in PublicEnsemble.tsx, /assignments?focus= scroll+flash) and useMonthSwipe all assume the page scrolls. A QBO-style shell with an inner-scrolling content well — the natural way to build the ≥1024px grid, and how the director side already works (.dir-content overflow-y:auto) — would break scroll restoration and change all sticky math on the public side. Note the router itself is a non-issue: the grid is contained in PublicLayout.tsx with zero route changes; the real risk is the scroll container choice.  
  *Mitigation:* Write it into Phase 1's spec: public desktop uses position:sticky sidebar in a grid column with body remaining the scroll root; inner-scroll wells are permitted only on the director surface. Add the deep-link anchor flows to the contract-regression checklist with desktop viewport included.
- **[MAJOR] The public/director code-split boundary is convention-only and the redesign's 'shared components' are aimed straight at it**  
  The split holds today solely because DirectorApp.tsx is the one lazy import: public code already imports ~20 modules from src/director/ (hooks/*, utils, types, Linkify, firebase — verified), which Rollup hoists into the public/shared chunk; nothing enforces the boundary (eslint.config.js has no no-restricted-imports, no bundle-size CI). Two concrete redesign traps: (a) new shared primitives (deterministic cover-art component, record panes, saved-view tables) built under src/director/ and imported by public pages will drag director CSS/JS into the parent-facing bundle — director.css alone is 1,885 lines that public users currently never download; (b) Phase 4's rail work invites converting DirectorApp's manual tab parsing (location.pathname.split('/')[2]) into nested routes declared in main.tsx, which would statically import every director view and destroy the lazy boundary entirely.  
  *Mitigation:* Before Phase 2 introduces the first shared component: create src/shared/ui/ as the only legal shared home, add eslint no-restricted-imports (src/public/** may import only director/{hooks,utils,types}), and a CI assertion on the public entry chunk size. Phase 4 rule: director routing stays inside the lazy chunk; the go()/intent contract needs no router changes anyway.
- **[MAJOR] Phase 1 (public desktop shell) is mis-sized: M as scoped, but the attached obligations make it an L**  
  No desktop breakpoint exists anywhere (only 640px overlay tweaks); Phase 1 as written also carries: the zoom-subtree DOM restructure (header/sidebar move outside .pub-app while TextSizeControl — which lives in that header — keeps targeting .pub-app via querySelector, and all overlays currently render inline inside the zoomed subtree, so chrome and overlays will scale differently at 1.15/1.3x); dark-token coverage for all new chrome (public dark mode is a token override in uiUpdates.css:338 — any hardcoded sidebar color mismatches); adding new chrome to three separately-enumerated print blocks; the 'mobile byte-for-byte unchanged' exit criterion; and the persistent header search field anchoring the existing full-screen SearchOverlay as a palette. Each is individually small; together they are the largest single release of the public track.  
  *Mitigation:* Either relabel Phase 1 as L and staff it accordingly, or split it: 1a = pin the existing hamburger menu open as a sidebar at ≥1024px inside the current DOM (no zoom-subtree move, no search field), 1b = chrome extraction from the zoom subtree + header search. Each half is independently revertible.
- **[MAJOR] 'Unify the three token sets' underestimates the append-only patch-layer minefield**  
  Live styling is resolved by source-order override chains across base sheets plus two append-only uiUpdates.css layers (469+594 lines, ~10 !important). Verified booby traps: the LIVE tab bar (.pub-tabbar) is defined only in public/uiUpdates.css while public.css still contains a dead .pub-nav block whose --pub-nav:62px token still feeds a base padding rule that the patch layer then re-overrides to 16px; the actual tab bar is 52px and main.tsx independently hardcodes 76px on the toast. index.css is a third dead-but-live-tokened layer. Flattening or 'reclaiming' these chains mid-redesign produces regressions that only manual visual inspection catches — and the claimed '~270 hardcoded px' understates the rem question: there are 2,613 px occurrences and 425 font-size px declarations, so the px→rem branch of Phase 0 is not remotely M-sized.  
  *Mitigation:* Do not flatten during the redesign. Freeze the existing layers, adopt CSS @layer (or at minimum per-phase new CSS files) so each phase's styles are one import and one git revert; kill the px→rem branch explicitly in favor of the chrome-outside-zoom-subtree decision; delete dead blocks only with per-selector grep evidence attached to the PR.
- **[MINOR] i18n verification deferred to Phase 7 leaves Spanish-speaking families with English chrome for most of the migration**  
  Phases 1–3 each ship new parent-facing strings (sidebar section headers, 'Show all N', 'You're all caught up', happening-now text, day-sheet labels). The EN/ES machinery (t()/useLang, ~107 keys) exists and the sidebar can reuse existing nav.* keys, but the plan only 'verifies EN/ES coverage' in Phase 7.  
  *Mitigation:* Make 'every new public-facing string has an EN and ES key' an exit criterion of each public phase (1, 2, 3, 7), not a Phase 7 sweep. Director-side strings can stay English per current scope.
- **[MINOR] Moving chrome outside the zoom subtree is itself a visible accessibility change — today the whole shell zooms**  
  Verified: TextSize.tsx sets zoom on .pub-app, which wraps header, menus, tab bar, overlays and content — so at 1.3x, low-vision users currently get enlarged 11px tab-bar labels and header controls. After the move, chrome text stays at 1.0x by design. Defensible ('Aa scales content only'), but it is a regression of current behavior for the users the control exists for, and nobody has decided it on purpose. Related dead code: applySavedTextSize() is exported but never called, so zoom applies only after React mounts (flash of unzoomed content at saved 1.3x).  
  *Mitigation:* Decide explicitly and note it in the Phase 0/1 spec; consider bumping base chrome font sizes (tab labels 11px→12-13px) as compensation; call applySavedTextSize() pre-render (or delete it) while touching the shell.
- **[MINOR] Print hiding is enumerated per-class in three separate files plus a body:has() scheme — every new chrome element needs 3-4 edits**  
  public.css:781, season.css:74, and startGuide.css:80 each hide chrome by explicit class list (.pub-header, .pub-tabbar, ...); qrKit.css:72 uses body:has() visibility scoping with a fragile specificity comment. New sidebar, mini-bar, action bar, and director rail must be appended to every applicable list or they print. Confirmed real: print blocks force backgrounds white but never reset --pub-ink/--pub-text, so the concept's forced-light print token reset is a genuine latent-bug fix, not busywork.  
  *Mitigation:* In Phase 0, add one shared .no-print utility plus a single global '@media print { .no-print{display:none!important} :root{light tokens} }' block, tag all existing and future chrome with it, and add 'print all four surfaces in dark mode' to the per-phase checklist.
- **[MINOR] New chrome and gradient heroes need explicit dark-mode and contrast coverage**  
  Public dark mode is only a ~20-line prefers-color-scheme token override; new sidebar/hero CSS using hardcoded colors will mismatch in dark mode, and director-set arbitrary ensembleColor hexes behind hero text can fail WCAG (the existing --pub-gold-text correction is the precedent). The concept's grafted luminance-aware text function is the right answer but is not assigned to a phase.  
  *Mitigation:* Build all new chrome on --pub-* tokens (dark comes free); land the luminance/desaturation text-color helper in Phase 3 before the first gradient hero ships, with a snapshot test across all current ensemble colors in both themes.

  *Green lights:* URL-contract freeze is genuinely cheap to honor: all 8 phases are layout/container work needing zero route edits · The director shell is unusually ready for Phase 4: DirectorApp.tsx is a 231-line shell where tab+intent live entirely in the URL via go()/DirNavigate, independe · The code-split boundary currently holds cleanly (verified: no director→public imports; director.css and its uiUpdates layer load only with the lazy chunk), so p · Drawer→slide-in-pane conversion (Phase 5) is a real container swap, not a rewrite: all forms/drawers are self-contained components launched from local state, an (+6 more in the session record)

## 8. Structural risks (from the design phase)

- No desktop breakpoint exists anywhere today (only 640px overlay tweaks in 5,924 lines of CSS): both shells (Phases 1 and 4) restructure PublicLayout.tsx and DirectorApp.tsx and realistically touch ~15 of 27 CSS files / 2,000+ lines — the single largest structural risk, and why foundations ship first with zero visual change.
- Text-size 'Aa' control applies CSS zoom to .pub-app: any fixed sidebar or happening-now bar inside that subtree gets zoomed and breaks at 1.3x. Mitigations (chrome outside the subtree, or a px→rem migration) are each sizable; skipping this check regresses a required accessibility feature.
- Print is load-bearing on four surfaces (concert program, season fridge copy, start guide, QR kit): every new chrome element needs @media print hiding, and today print forces white backgrounds but not text colors — any theming work before the forced-light print block ships risks printing near-invisible ink on the physical concert program.
- Bottom-edge sticky collisions: public tab bar, happening-now bar, SW update toast, director FAB, finish-roll bar, and WriteTray all compete for the same edge. Without the one-sticky-per-edge budget and a formalized z-index scale, Phases 3/6 will produce overlapping chrome on small phones.
- Mid-year muscle memory (QuickBooks Online's force-migration backlash is the cautionary tale): parents have printed QR posters and trained habits, the director has in-term workflows. Public mobile must change almost nothing; the director shell swap should land over summer break, never mid-semester, and never reorder the 4 public tabs.
- Change-awareness without accounts rests on localStorage last-seen heuristics: shared family devices, incognito mode, and cleared storage produce wrong unread states. Indicators must degrade to neutral (no badge) rather than false-positive, and can never replace GlobalAlerts for safety-critical cancellations.
- Data-driven ensemble hex colors as gradient heroes risk WCAG contrast failures (Spotify's own documented weakness): text-on-gradient needs computed luminance handling like the existing --pub-gold-text correction, and gradients must stay print-legible on QR posters.
- SaaS-pattern scope creep: saved-view governance, customizable dashboards, notification preference matrices, and multi-level nav are exactly the criticized artifacts of the reference products at scale — this app serves roughly one director and a few hundred families; every configurable surface should ship as a fixed configuration instead.
- Offline PWA fragility: the shell restructure changes the cached app shell (sw.js 'nwsa-hub-v1'), manifest theme colors, and toast anchoring; a botched cache transition strands offline users on stale chrome — cache versioning and update-toast behavior need explicit re-verification each phase.
- Contract breakage during refactor: vanity slugs, /nwsa-music-hub basename, hash anchors, ?focus/?ensemble deep links, static ICS feed URLs, the single public PlannedAbsence write, and the code-split public/director bundles are all easy to silently break while moving shell code — the Phase 0 regression checklist must gate every phase.
- The exact school name is a shipped-bug class: any new masthead, hero, or print header must render 'New World School of the Arts' / 'NWSA' verbatim (grep before writing, per CLAUDE.md — the printed program got this wrong once already).

## 9. Decisions needed from the owner

- Theme direction: the recommendation is light-first (Basecamp/QBO calm) with dark mode remaining an automatic prefers-color-scheme variant — but do you want a Spotify-dark default anywhere (e.g., piece pages only), and should there be a manual light/dark toggle instead of OS-sync only?
- Photos and artwork: do ensemble/concert photos exist or will someone maintain them? Airbnb-style photo-led cards and hero mosaics only make sense if yes; otherwise the generated color-gradient + monogram system is the permanent design, not a fallback.
- Device reality check: can you confirm (from any analytics or lived experience) that parents/students are overwhelmingly on phones, and whether the director actually has a laptop/iPad at the podium? This decides how much Phase 1 (public desktop) and Phases 4–6 (director desktop) are actually worth.
- Nav attachment: are you attached to the current four public tabs (Home/Calendar/My Schedule/Concerts) and the hamburger? Would you swap 'Concerts' for 'Ensembles' as a tab, or is the season page the more-visited destination?
- Rollout timing: is a summer big-bang acceptable for the director shell, and are mid-year additive-only changes acceptable for the public surface — or must everything wait for the term boundary?
- Change indicators for families: do you want Slack-style 'new since last visit' dots/Updates feed at all, or is the existing GlobalAlerts + banners posture (calm, no unread mechanics) the intended ceiling?
- Brand constraints: beyond teal #0d7e8e, logo purple, and concert gold — does New World School of the Arts impose sanctioned colors, typography, or logo-usage rules the redesign must follow on public pages and printed output?
- i18n scope: many public strings are still hardcoded English and the director side is untranslated — should the redesign budget include closing the EN/ES gap (recommended for every NEW string at minimum), and does the director console need Spanish at all?
- Happening-now bar scope: personalized only (saved student's NOW/NEXT) or also a school-wide mode ('Next today at NWSA: Jazz Band 3:30') for devices with no saved identity?
- Director keyboard investment: will roll ever realistically be taken on a keyboard device, or should Phase 6's keyboard-chord work be cut in favor of more phone-roll polish (e.g., seating tap-roll improvements)?

## 10. Decisions recorded (July 11, 2026)

The owner answered the four build-shaping questions:

1. **Devices** — families are mostly on phones; the director uses
   laptops/desktops *and an iPad daily*. Consequence: the director "desktop"
   layout (≥1024px rail + panes + tables) must be **coarse-pointer-first** —
   iPads in landscape hit the desktop breakpoint, so every desktop affordance
   needs 44px touch targets and zero hover dependencies (this hardens the
   already-adopted "no hover-only reveals" amendment from suggestion to
   foundation). The public desktop shell drops in priority relative to the
   director track; keyboard roll chords are cut (touch-first roll polish wins).
2. **Theme** — light-first agreed, "but a bit more darkness to diversify
   color." Consequence: keep content surfaces light and print-safe, but deepen
   the chrome and accent ramp — a darker ink-teal header/rail, richer second
   stops in the ensemble gradients, and a deeper neutral text scale — so the
   palette reads dimensional rather than washed-out. No Spotify-dark surfaces.
3. **Artwork** — some photos may exist; gradients are the consistent bet, and
   generic music/ensemble photography is acceptable. Consequence: the
   deterministic gradient + monogram system is the permanent primary; an
   optional photo layer (generic instrument/stage imagery) may back heroes
   where available, always behind the computed-contrast scrim, never required,
   never blocking.
4. **Timing** — owner asked to compress the rollout into ~one week (July),
   instead of term-boundary phasing. Assessment: July is *exactly* the safe
   window the critics demanded — school is out, so the "never change director
   chrome mid-term" and family-muscle-memory risks largely evaporate, and a
   single compressed release burst replaces eight service-worker cache churns,
   with a month of soak before school resumes in August. The costs: phases get
   days-not-weeks of individual soak (bugs surface in bulk during the
   verification pass instead of gradually), the verification matrix (real
   phone at ES/1.3× zoom, real iPad, four print surfaces in both OS themes,
   PWA cache transition) becomes the true bottleneck and needs owner
   spot-checks on real devices, and the two L-sized phases make ~7 days
   realistically 7–10. Compression changes the calendar, not the order:
   foundations still ship first and every gate still applies.

### Compressed schedule (order preserved, calendar compressed)

- **Day 1 — Foundations:** base.css extraction; token unification + deepened
  palette; `t()` interpolation; luminance text-color helper; forced-light
  print block + `.no-print`; z-index scale; SW build-hash + lazy-chunk 404
  recovery; bottom-slot manager; import-boundary lint; regression checklist.
- **Day 2 — Public desktop shell** (1a + 1b, zoom architecture decided and
  implemented; mobile byte-for-byte).
- **Day 3 — Detail patterns:** sticky day-sheet; stacked mobile action bar;
  the Add-to-calendar fix (Google template URL + success feedback + ES).
- **Day 4 — Hubs + Spotify moments:** tool-tile hubs, gradient heroes with
  computed contrast + print mode, piece page (conditional album grammar),
  happening-now mini-bar.
- **Day 5 — Director shell, iPad-first:** grouped rail, "+ New" with
  contextual primary, always-expanded frequency-ordered phone/iPad menu,
  Cmd+K, gear split.
- **Day 6 — Director data surfaces:** saved-view chip subset + bulk bar;
  RecordPane/FormPane primitives; slide-in panes; schedule two-pane; Take Roll
  strip (exception-derived, no-reflow filtering).
- **Day 7+ — Verification:** device matrix, four print surfaces light + dark,
  PWA transition, EN/ES sweep, school-name grep gate, fixes.
- **Deferred to fall:** Phase 7 change-awareness layer; keyboard chords (cut).

---

*Study artifacts: the interactive wireframe version of this study is published
as a Claude artifact; the full agent outputs (reference teardowns, all four
concepts, judge scorecards, critic reports) are preserved in the session that
produced this document.*
