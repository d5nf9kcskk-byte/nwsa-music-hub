# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# This repo is public. Never commit credentials, bulletins, or student data.
*service-account*.json
*-adminsdk-*.json
.env
.env.*
*.pdf

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Per-session git worktrees (see the worktree rule at the top of CLAUDE.md).
# Each is a full nested checkout; without this they show as untracked
# directories in the main checkout, one `git add -A` away from being
# committed as a gitlink.
.claude/worktrees/
