{
  "name": "nwsa-hub-functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=lib/index.js --external:firebase-functions --external:firebase-admin",
    "selfcheck": "node --experimental-strip-types src/lessonsFeed.selfcheck.ts && node --experimental-strip-types src/concertCheckin.selfcheck.ts && node --experimental-strip-types src/concertTally.selfcheck.ts"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
